[pytest]
testpaths = tests
pythonpath = src
addopts = -v --tb=short
asyncio_mode = auto
markers =
    textual_integration: Textual app.run_test() + Pilot integration tests (slow, opt-in via --run-textual-integration)
