---
id: "d0d1"
status: closed
deps: []
links: []
created: 2026-08-03T18:45:45Z
type: task
priority: 2
closed_at: 2026-08-03T18:52:06Z
resolution: completed
close_reason: "README now uses the supported bare council chat invocation and a public-doc regression rejects the stale option."
closed_context: codex:a775e0448cdb5a76
---
# Fix stale council chat --new README example

## Acceptance Criteria

- [x] The README uses bare `kd council chat` for the supported new-thread path.
- [x] A public-documentation contract rejects the stale `--new` example.
- [x] Manual checkout CLI output proves `--new` is rejected and bare chat is the
  documented path in command help.

## Context

Ticket `990d` manual release review compared the README with
`uv run kd council chat --help` and `uv run kd council chat --new`. Help exposes
an optional thread ID and no `--new` flag; the latter command exits with “No such
option: --new.” Source and existing TUI tests confirm that omitting the thread ID
creates a new thread.

## Worklog

- 2026-08-03 — Fixed in the 990d release-documentation slice by replacing the
  stale option with the supported bare invocation and adding a focused public-doc
  regression. Parent review should close this ticket with the integrated 990d
  documentation commit rather than creating a second implementation change.
- [14:52] [codex:a775e0448cdb5a76] — Closed: README now uses the supported bare council chat invocation and a public-doc regression rejects the stale option.

## Lifecycle

- 2026-08-03T18:52:06Z [codex:a775e0448cdb5a76] — closed (completed): README now uses the supported bare council chat invocation and a public-doc regression rejects the stale option.
