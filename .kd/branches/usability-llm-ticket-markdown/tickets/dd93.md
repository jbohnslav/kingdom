---
id: "dd93"
status: closed
deps: []
links: []
created: 2026-05-01T12:10:32Z
type: task
priority: 2
closed_at: 2026-05-01T12:14:24Z
---
# Address PR review feedback before merge

Address the actionable GitHub PR review feedback on PR #42 for branch usability-llm-ticket-markdown.

Scope:
- Make remove_worktree() require an explicit feature argument so future callers cannot silently resolve the wrong worktree path.
- Fix resolve_invocation_git_root() so peasant commands invoked from nested Git repos/submodules still operate on the Kingdom project repo.
- Add timeout=10 to cleanup_accepted_peasant() git branch -D subprocess call.
- Update the resolve_invocation_git_root() docstring so it accurately describes the fallback behavior.
- Document the surprising -t overloading behavior for kd tk create where appropriate.
- Document the intentional resolve_current_run() priority inversion in the PR description, changelog, or project docs if this branch has a suitable place for user-visible behavior notes.

Source: GitHub PR #42 comments and review threads fetched on 2026-05-01.

## Acceptance Criteria

- [x] remove_worktree() feature fallback is removed or made loudly impossible, and all callers/tests are updated.
- [x] resolve_invocation_git_root() chooses the Kingdom project repo even when invoked below a nested Git repo/submodule, with regression coverage.
- [x] cleanup_accepted_peasant() git branch deletion subprocess has timeout=10.
- [x] Relevant docs/docstrings cover the PR feedback notes for -t overloading and resolve_current_run() behavior.
- [x] Full test suite passes before closing.

## Worklog

- [2026-05-01 08:13] — Added failing regressions before implementation for the PR review bugs:

  - remove_worktree() without feature now has a unit test expecting a TypeError, proving callers must provide the owning feature explicitly.
  - resolve_invocation_git_root() now has a unit test for invocation below a nested Git repo inside the Kingdom checkout; it should return the Kingdom project root, not the nested repo.

  Both tests failed before the fix. Implemented the fixes, added timeout coverage for accepted peasant branch deletion, and added the requested documentation/docstring notes.
- [2026-05-01 08:14] — Verification complete:

  - uv run pytest tests/test_worktree.py::TestRemoveWorktree tests/test_cli_peasant.py::TestProjectRootDiscovery tests/test_cli_peasant.py::TestPeasantReview::test_review_accept_removes_worktree_and_deletes_branch tests/test_cli_peasant.py::TestPeasantReview::test_review_accept_cleanup_failure_warns_but_succeeds => 6 passed.
  - uv run ruff check . => all checks passed.
  - uv run pytest => 1881 passed, 38 skipped, 1 xfailed.

  Could not run ty because `uv run ty check` failed to spawn `ty` (not installed in this environment).
