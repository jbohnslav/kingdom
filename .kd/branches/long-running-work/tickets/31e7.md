---
id: "31e7"
status: closed
deps: []
links: []
created: 2026-03-06T13:56:12Z
type: task
priority: 2
closed_at: 2026-03-06T15:28:31Z
assignee: peasant-31e7
---
# Improve peasant accept merge conflict workflow and recovery

The current peasant accept flow breaks badly on merge conflicts:

1. accept tries to merge ticket/<id> into the feature branch
2. If conflict, it exits code 1 and tells you to resolve in the worktree — but that's backwards (the merge happens on the feature branch, not in the worktree)
3. After manual resolution, accept can't be re-run because it blindly retries the merge
4. This leads to manual cleanup (kd tk close + kd peasant clean --force) which bypasses session state updates

What went wrong in practice:
- Uncommitted ticket file changes on long-running-work caused the merge to be messy
- The recovery steps in the error message are confusing (says cd into worktree, but the conflict is on the feature branch)
- After manually resolving and committing the merge, there's no way to tell accept 'merge is done, just do the cleanup'

Needs:
- Document the correct flow: commit/stash feature branch changes BEFORE running accept
- Make accept idempotent: if the worktree branch is already merged, skip the merge and proceed with cleanup (close ticket, update session, remove worktree)
- Fix the error message recovery steps to match what actually needs to happen
- Update the skill (SKILL.md) so agents (lord, Hand) know the right conflict resolution flow

## Acceptance Criteria

- [ ] kd peasant accept is idempotent — if worktree branch already merged, skips merge and does cleanup
- [ ] Error message recovery steps accurately describe how to resolve conflicts
- [ ] SKILL.md documents the correct merge conflict workflow for agents
- [ ] Uncommitted changes on feature branch are handled gracefully (warn or stash before merge)

## Worklog

- 2026-03-06 09:11 — Documented the actual merge conflict resolution workflow from accepting dfbb:

  ## What happened (dfbb accept — council.py + test_cli_council.py conflicts)

  1. Ran `kd peasant accept dfbb` from the main repo (/Users/jrb/code/kingdom)
  2. Accept detected merge conflicts in 2 files: src/kingdom/cli/council.py (3 conflict regions) and tests/test_cli_council.py (4 conflict regions)
  3. Accept exited code 1, leaving the worktree in a merge state (UU files visible in worktree's git status)

  ## Correct resolution flow (what actually worked this time)

  1. Read the conflicted files in the worktree (.kd/worktrees/dfbb/...) using absolute paths
  2. Understood both sides: dfbb added --all/archive support, long-running-work (06a3) added --json output
  3. Edited the files in the worktree to combine both features — kept both params, handled the 4-way matrix (json+all, json+default, rich+all, rich+default)
  4. Verified: `python -c 'import py_compile; py_compile.compile(...)'` for syntax, then full pytest
  5. Staged and committed the merge resolution in the worktree: `git add <files> && git commit`
  6. Ran `kd peasant accept dfbb` again from the main repo — this time it succeeded (worktree branch was already merged)

  ## Key insight: accept IS idempotent for the merge step
  When I re-ran accept after manually resolving and committing the merge, it detected the branch was already merged and proceeded with cleanup. So the idempotency AC may already be partially met — needs verification.

  ## What's still broken
  - The error message from the first accept attempt (when conflicts are detected) needs better recovery instructions
  - Should explicitly say: 'resolve conflicts in .kd/worktrees/<id>/, commit there, then re-run kd peasant accept'
  - Should warn about uncommitted changes on the feature branch before attempting merge
- 2026-03-06 09:22 — Found another gap: `kd peasant clean` fails with 'No worktree found' when the worktree is already removed but session files (.kd/.../sessions/peasant-<id>.json) still exist. Had to manually rm the session + lock files to clear 06a3 from `kd peasant status`. Clean should handle this gracefully — if worktree is gone, just remove session state.
- 2026-03-06 10:15 — Another successful merge conflict resolution via the same pattern: resolve in main repo, commit, re-run accept. This time config.json only (trivial). Accept idempotency confirmed working for the third time.
- 2026-03-06 10:15 — Confirmed: the recovery steps in the error message are wrong. They say:
    1. cd into worktree
    2. git merge long-running-work
    3. retry accept

  But the merge actually happens on the feature branch (main repo), not in the worktree. The correct flow is:
    1. Resolve conflict markers in the main repo files
    2. git add + git commit (completes the merge on the feature branch)
    3. Re-run kd peasant accept (idempotent — detects branch already merged, does cleanup)

  The error message recovery steps need to be rewritten to match reality.
- [10:16] — Iteration 1/50 — calling agent
  Ticket: Improve peasant accept merge conflict workflow and recovery
- [10:17] — Iteration 1/50 — calling agent
  Ticket: Improve peasant accept merge conflict workflow and recovery
- 2026-03-06 10:20 — Implemented all four acceptance criteria:

  1. **Idempotent accept**: Before merging, checks `git merge-base --is-ancestor` to detect if ticket branch is already merged. If yes, skips merge and proceeds to cleanup (close ticket, update session). This handles the re-run-after-manual-resolution case.

  2. **Uncommitted changes check**: Before attempting merge, calls `check_uncommitted_changes(base)`. If dirty, refuses with clear message telling user to commit or stash first.

  3. **Fixed error message**: Recovery steps now correctly reference the feature branch working tree (not the worktree) and describe: resolve markers → git add + commit → re-run accept.

  4. **SKILL.md updated**: Added 'Merge Conflict Recovery' section under 'When kd Says No' with the correct workflow for agents.

  All 1710 tests pass. 3 new tests added for the new behavior.
- [10:21] — Now let me understand the merge logic. The accept command at line 1496 runs `git merge ticket/<id> --no-edit` on the feature branch. If the branch is already merged, git merge will succeed (it'll say "Already up to date"), so idempotency for that case already works. But if there's a merge conflic...
- [10:21] — Files changed:
  skills/kingdom/SKILL.md    |  16 ++++++
   src/kingdom/cli/peasant.py |  50 +++++++++++++------
   tests/test_cli_peasant.py  | 122 +++++++++++++++++++++++++++++++++++++++++++++
   3 files changed, 172 insertions(+), 16 deletions(-)
- [10:22] — Council review: APPROVED — awaiting king review
  [claude] APPROVED: Good. Let me verify a couple of things — the `check_uncommitted_changes` function and how `git_branch_name` is defined in context.Wait — I'm reading the **base branch** version, not the worktree version. Let me read from the worktree.Now let me check for one important edge case — what happens when `git merge` fails with conflicts and `capture_output=True`. Does git leave conflict markers in the working tree even when output is captured?Good — uses `git status --porcelain`, returns l...
  [codex] APPROVED: I’m reviewing the `ticket/31e7` patch directly: diff, touched files, and the surrounding accept flow in `peasant.py`, then I’ll run targeted checks if the implementation or tests leave any gaps.
