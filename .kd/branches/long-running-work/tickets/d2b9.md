---
id: "d2b9"
status: in_review
deps: []
links: []
created: 2026-03-03T21:40:25Z
type: feature
priority: 2
assignee: peasant-d2b9
---
# Epic support: hierarchical ticket decomposition

Add epic support to the ticket system. An epic is a ticket with `type=epic` — an explicit opt-in label. Any ticket can have children via the existing `parent` field, but only `type=epic` tickets get epic-specific guardrails (peasant rejection, close enforcement). No new entity or storage model.

Key changes:
1. Validate ticket type on create/update against a known set (task, bug, feature, epic). Currently accepts any string.
2. Add `kd tk list --parent <id>` to list children of a ticket.
3. Add aggregate child status to the existing Children section in `kd tk show` (e.g. "3/5 closed"). Children already render; this adds the rollup.
4. Guard `kd peasant start` against `type=epic` tickets (epics aren't atomic work units).
5. `kd tk close` on a `type=epic` ticket refuses if children are still open (not just a warning — hard block). Note: `kd done` already blocks on open branch tickets, so no separate check needed there.

Non-goals:
- No separate Epic model or storage — epics are tickets
- No dependency changes — deps remain between individual tickets only
- No automatic epic suggestion or detection in the skill
- No special backlog treatment for epics
- No `kd tk split` command in the first version
- No epic-specific behavior for non-epic tickets that happen to have children

## Acceptance Criteria

- [ ] Ticket type validated against known set (task, bug, feature, epic) on create and update
- [ ] `kd tk list --parent <id>` shows children of a ticket
- [ ] `kd tk show` on a ticket with children displays aggregate status rollup (e.g. "3/5 closed")
- [ ] `kd peasant start` rejects `type=epic` tickets with a clear error
- [ ] `kd tk close` on a `type=epic` refuses if any children are still open
- [ ] `kd tk list` shows `[epic]` badge in the Title cell for `type=epic` tickets

## Worklog

- 2026-03-03 16:57 — Scope narrowed during epic design discussion: dropped the proposed kd tk split command from the first version. First pass stays focused on explicit type=epic, child listing/rollup, peasant rejection, and close enforcement. Existing tickets can still be reshaped manually when needed; no dedicated conversion UX yet.
- 2026-03-06 10:24 — Council reviewed CLI display options. Consensus: keep default list simple, no new columns or tree view. King's call: add a subtle [epic] badge in the Title cell for epic rows (Codex's suggestion). Added to ACs.
- [10:25] — Iteration 1/50 — calling agent
  Ticket: Epic support: hierarchical ticket decomposition
- 2026-03-06 10:32 — Implemented all 6 ACs: TICKET_TYPES validation on create, --parent list filter (implies --all), child rollup in show, peasant epic guard, close guard for epics with open children, [epic] badge in list. 15 new tests in test_epic.py, all passing. Full suite green (1723 passed).
- [10:32] — Let me read the ticket and explore the relevant codebase areas in parallel.Good overview. Let me read the key files I need to modify.Let me read the specific sections I need to modify.Now let me check the ticket update/edit command and existing tests.Now let me look at the ticket edit command and...
- [10:32] — Files changed:
  src/kingdom/cli/peasant.py |   7 ++
   src/kingdom/cli/ticket.py  |  37 +++++++-
   src/kingdom/ticket.py      |   3 +-
   tests/test_epic.py         | 211 +++++++++++++++++++++++++++++++++++++++++++++
   4 files changed, 253 insertions(+), 5 deletions(-)
- [10:34] — Council review: BLOCKING (bounce 1/3) — returning to working
  [codex] BLOCKING: I’m reviewing the `ticket/d2b9` patch directly: diff, touched files, and the surrounding ticket/peasant flows. I’ll run targeted checks only if the implementation or tests look incomplete.
  [claude] APPROVED: Good start. Let me check a few specific areas more carefully.Now let me check whether `ticket_edit` also validates the type — the AC says "on create **and update**."That's a raw editor command — no validation. But there's also `ticket_update`:There's no `ticket_update` or `ticket_set` command that lets you change the type programmatically. The only way to change type is via `ticket_edit` (raw editor), which doesn't validate. The AC says "on create and update" — but there's no progra...
- [10:34] — Iteration 2/50 — calling agent
  Bouncing on council feedback (1 blocking, 1 approved) — see review above
- 2026-03-06 10:35 — Council bounce fix: added post-edit type validation to kd tk edit. After the editor closes, the ticket is re-read and rejected if type is not in TICKET_TYPES. Added 2 tests (edit_rejects_invalid_type, edit_accepts_valid_type). Full suite green: 1725 passed.
- [10:35] — The council's blocking issue is that type validation only runs on `kd tk create`, not on update. The `kd tk edit` command opens the file in $EDITOR but doesn't validate the result afterward. I'll add post-edit validation to `ticket_edit` that re-reads the ticket after the editor closes and reject...
- [10:35] — Files changed:
  src/kingdom/cli/ticket.py |  9 +++++++++
   tests/test_epic.py        | 25 +++++++++++++++++++++++++
   2 files changed, 34 insertions(+)
- [10:36] — Council review: APPROVED — awaiting king review
  [codex] APPROVED: I’m reviewing the bounce fix in the ticket worktree now, focusing on `ticket_edit` and the new tests. The earlier gap was narrow, so I only need to verify that the post-edit validation actually covers the update path without breaking normal editing.
  [claude] APPROVED: I reviewed the first two commits in the previous round and approved. The new commit `59effe0` addresses the bounce — post-edit type validation. Let me inspect that fix.Clean fix. Let me verify one thing — the `read_ticket` import.Good — `read_ticket` is imported at line 47. The fix is correct.
