---
id: "0f6f"
status: closed
deps: []
links: []
created: 2026-03-06T17:19:03Z
type: task
priority: 2
assignee: peasant-0f6f
parent: 1ba2
---
# Add kd tk parent command to reparent existing tickets

Add a command to set/change the parent of an existing ticket. Currently --parent only exists on create, so you have to manually edit frontmatter to reparent. Proposed interface: `kd tk parent <ticket-id> <epic-id>` to set, `kd tk parent <ticket-id> --clear` to remove.

## Acceptance Criteria

- [ ]

## Worklog

- [12:20] — Iteration 1/50 — calling agent
  Ticket: Add kd tk parent command to reparent existing tickets
- 2026-03-06 12:23 — Added ticket_parent command to cli/ticket.py. Set parent: kd tk parent <id> <parent-id>. Clear parent: kd tk parent <id> --clear. Guards: no self-parent, no conflicting set+clear, requires either parent_id or --clear. 6 tests in test_cli_ticket_lifecycle.py. All 1780 tests pass, lint clean.
- [12:23] — Good, I have a clear picture. Now let me add the `parent` command. I'll model it after the `assign`/`unassign` pattern but combine set and clear into one command.Now let me test it manually and run the test suite.The `kd` command is installed from the main repo, not the worktree. Let me check how...
- [12:23] — Files changed:
  src/kingdom/cli/ticket.py          | 43 +++++++++++++++++++
   tests/test_cli_ticket_lifecycle.py | 88 ++++++++++++++++++++++++++++++++++++++
   2 files changed, 131 insertions(+)
- [12:26] — Council review: APPROVED — awaiting king review
  [codex] APPROVED: I’m reviewing the `ticket/0f6f` patch directly now. This is a small CLI addition, so I’m checking the command semantics, guardrails, and the targeted lifecycle tests rather than broad project state.
  [claude] APPROVED: Let me check `resolve_ticket_or_exit` since it's used with a `not_found_label` kwarg.Good — `resolve_ticket_or_exit` already supports the `not_found_label` kwarg. Now let me verify `write_ticket` is imported in the diff context.All imports are present. Let me check one edge case — does the command handle parent_id that doesn't match any ticket?
