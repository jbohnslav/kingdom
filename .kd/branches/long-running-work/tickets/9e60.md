---
id: "9e60"
status: closed
deps: []
links: []
created: 2026-03-06T16:24:32Z
type: task
priority: 2
assignee: peasant-9e60
parent: 4178
---
# Lord plumbing: stopping status, ready semantics, CLI scaffolding

Prerequisites for lord mode. Three changes: (1) Add 'stopping' to persisted session statuses in session.py so kd lord stop can signal graceful shutdown. (2) Change ready semantics at the model level — filter_tickets_by_deps(..., ready=True) in ticket.py must exclude in_progress tickets. Ready means 'startable', not 'startable or already started'. Update model, CLI, and tests. (3) Add kd lord start <epic-id> and kd lord stop CLI entry points (scaffolding only — the actual agent logic is a separate ticket). The lord runs directly on the feature branch (no worktree), since all code work is done by peasants in worktrees.

## Acceptance Criteria

- [ ] stopping is a valid persisted session status in session.py
- [ ] filter_tickets_by_deps(..., ready=True) in ticket.py excludes in_progress tickets (model-level change, not just CLI)
- [ ] Existing tests updated to reflect new ready semantics
- [ ] kd lord start <epic-id> CLI entry point exists (can be a stub that prints 'not yet implemented')
- [ ] kd lord stop CLI entry point exists

## Worklog

- [11:32] — Iteration 1/50 — calling agent
  Ticket: Lord plumbing: stopping status, ready semantics, CLI scaffolding
- 2026-03-06 11:35 — All 5 AC items implemented in a single commit: (1) 'stopping' added to AGENT_STATUSES, (2) filter_tickets_by_deps ready=True now excludes in_progress (model + CLI), (3) tests updated for new ready semantics, (4-5) kd lord start/stop CLI stubs added. 1728 tests pass, linter clean.
- [11:35] — Let me read the ticket and the relevant source files to understand what needs to change.Let me start with the changes. First, add 'stopping' to session statuses.Also update the docstring:Now change `filter_tickets_by_deps` to exclude `in_progress` from ready:Now update the CLI status command's `r...
- [11:35] — Files changed:
  src/kingdom/cli/__init__.py |  6 ++++--
   src/kingdom/cli/lord.py     | 25 +++++++++++++++++++++++++
   src/kingdom/session.py      |  3 ++-
   src/kingdom/ticket.py       |  4 ++--
   tests/test_ticket.py        | 27 +++++++++++++++++++--------
   5 files changed, 52 insertions(+), 13 deletions(-)
- [11:36] — Council review: APPROVED — awaiting king review
  [codex] APPROVED: I’m reviewing the actual `ticket/9e60` patch now: the session status change, the ready-model change, and the new lord CLI stubs. I’ll only rerun targeted tests if the diff or existing coverage leaves something unclear.
  [claude] APPROVED: Clean and minimal. Let me verify the lord CLI actually works and check one edge case.That's because I'm running from the main repo, not the worktree. Let me check from the right location.The import fails because the installed kingdom package doesn't have lord.py yet (it's only in the worktree). That's expected — the file exists in the worktree but isn't installed. Let me just verify the file and the session changes directly.Good. Note that `ACTIVE_PEASANT_STATUSES` (line 208) does *...
