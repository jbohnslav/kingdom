---
id: "06a3"
status: closed
deps: []
links: []
created: 2026-03-06T12:26:54Z
type: task
priority: 2
closed_at: 2026-03-06T13:49:15Z
assignee: peasant-06a3
---
# Add --json output to agent-facing kd commands

Agents (lord, peasants, the Hand) constantly parse rich table output. Add `--json` to all commands that agents commonly consume. This is foundational for lord mode but also improves the experience for any agent-driven workflow.

Commands already with --json: `kd tk list`, `kd tk show`, `kd tk current`, `kd status`, `kd council ask`.

## Acceptance Criteria

- [ ] `kd peasant status --json` outputs JSON (must include effective status — report "dead" not "working" for dead processes)
- [ ] `kd peasant show --json` outputs JSON
- [ ] `kd council list --json` outputs JSON
- [ ] `kd council status --json` outputs JSON
- [ ] `kd deps tree --json` outputs JSON

## Worklog

- [08:11] — Iteration 1/50 — calling agent
  Ticket: Add --json output to agent-facing kd commands
- 2026-03-06 08:35 — Implemented --json for all 5 commands. All tests pass (1668 passed). Manually verified each command produces valid JSON output. peasant_status uses effective status (dead detection). peasant_show includes worklog/activity/commits. council list/status serialize thread and member states. deps tree produces recursive nested JSON.
- [08:35] — Let me find the specific commands I need to modify.Now let me read the specific functions I need to modify.Now let me read the rest of `peasant_show` and `print_thread_status`, plus look at how existing `--json` is done in the codebase.Now let me look at existing --json patterns to follow the sam...
- [08:35] — Files changed:
  src/kingdom/cli/council.py |  69 ++++++++++++++++++++++-
   src/kingdom/cli/peasant.py | 134 ++++++++++++++++++++++++++++-----------------
   src/kingdom/cli/ticket.py  |  34 ++++++++++++
   3 files changed, 185 insertions(+), 52 deletions(-)
- [08:37] — Council review: BLOCKING (bounce 1/3) — returning to working
  [claude] BLOCKING: Now let me run the tests in the worktree.All pass. Let me check for one thing — the `peasant_show` refactor changed the display behavior for the non-JSON path.Now let me check one subtle thing — the `peasant_show` refactor changed behavior when `agent_live_log` exists but is empty and there's no activity.There's a subtle behavioral change here. Before: if the log file existed but had no parseable activity lines, it would show nothing (fell through the `if` block). After: if the log ...
- [08:37] — Iteration 2/50 — calling agent
  Bouncing on council feedback (1 blocking, 0 approved) — see review above
- 2026-03-06 08:41 — Iteration 2: Added 12 tests across 3 test files covering all 5 --json commands. Tests verify JSON validity, field names, edge cases (empty lists, dead process detection, cycles). Also fixed deps_tree root duplication by reusing build_tree() for the root node. Full suite: 1680 passed.
- [08:41] — The council blocked because there are no tests for the five new `--json` flags. I need to add at least one happy-path test per command, plus a test for the dead-process detection in `peasant status --json`. Let me also fix the `deps_tree` root duplication the reviewer flagged.Let me look at how e...
- [08:41] — Files changed:
  src/kingdom/cli/ticket.py     |  11 +---
   tests/test_cli_council.py     |  98 +++++++++++++++++++++++++++++++++
   tests/test_cli_peasant.py     | 122 ++++++++++++++++++++++++++++++++++++++++++
   tests/test_cli_ticket_deps.py |  68 +++++++++++++++++++++++
   4 files changed, 289 insertions(+), 10 deletions(-)
- [08:42] — Council review: APPROVED — awaiting king review
  [claude] APPROVED: Now let me run the full test suite in the worktree to confirm everything passes.All 1680 tests pass. The blocking issue from the previous review (missing tests) has been addressed. Let me verify the deps_tree root fix is correct.---
