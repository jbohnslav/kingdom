---
id: "0aa6"
status: closed
deps: []
links: []
created: 2026-03-06T18:06:23Z
type: task
priority: 2
assignee: peasant-0aa6
parent: 1eeb
---
# Lord worklog hygiene: epic worklog as decision history, not transcript

The lord logs to the epic worklog every cycle, even idle ones. Result: verbose repetitive entries ("still awaiting_council, nothing to do") that drown out real progress. The epic worklog should be durable decision history, not a live transcript.

## Design (council-confirmed)

**Log only durable events:**
- Peasant launches
- Accepts / rejects with rationale
- Council bounces
- Escalations to the King
- Merge conflict resolutions
- Blocked states
- Epic completion
- Stop/start lifecycle events

**Keep verbose per-cycle narration in `agent-live.log`**, not the ticket worklog.

**Implementation:** One prompt line in the lord system prompt telling it when to log. Combined with 2ca9 (idle detection), most idle cycles won't reach the agent anyway — this handles the remaining edge cases.

**Note:** Depends on 2ca9. After implementing idle detection, re-run the lord and verify whether worklog noise is still a problem before adding guardrails. The prompt tweak may be unnecessary if idle cycles are eliminated.

## Acceptance Criteria

- [ ] Lord prompt instructs agent to only log on durable state changes (not idle observations)
- [ ] Epic worklog contains only actionable entries after a full lord run
- [ ] Per-cycle narration preserved in agent-live.log for debugging

## Worklog

- [10:43] — Iteration 1/50 — calling agent
  Ticket: Lord worklog hygiene: epic worklog as decision history, not transcript
- 2026-03-07 10:45 — Implemented worklog hygiene. Two changes: (1) Added 'Worklog Hygiene' section to lord prompt listing exactly which events warrant a worklog entry — peasant launches, accepts/rejects, council bounces, escalations, merge conflicts, blocked states, epic completion. (2) Replaced harness-level 'Cycle N — calling lord agent' worklog entry with logger.info so it goes to agent-live.log instead. All 1803 tests pass.
- [10:46] — I've read the ticket and the lord harness code. There are two sources of worklog noise: (1) the harness itself appends "Cycle N — calling lord agent" every active cycle, and (2) the lord agent's full summary gets appended every cycle without guidance about what's worth logging. I'll fix both.
- [10:46] — Files changed:
  src/kingdom/lord_harness.py | 16 +++++++++++++++-
   1 file changed, 15 insertions(+), 1 deletion(-)
- [10:46] — Council review: BLOCKING (bounce 1/3) — returning to working
  [codex] BLOCKING: I’m reviewing the `ticket/0aa6` patch directly now. The main question is whether the change actually enforces worklog hygiene, or just asks the agent nicely while the harness still appends per-cycle summaries.
  [claude] APPROVED: Clean, minimal diff. Two changes:
- [10:46] — Iteration 2/50 — calling agent
  Bouncing on council feedback (1 blocking, 1 approved) — see review above
- 2026-03-07 10:47 — Council bounce fix: removed the unconditional extract_lord_summary → append_lord_worklog path (line ~598). The harness no longer writes the lord's freeform summary to the epic worklog — it goes to logger.info (agent-live.log) instead. The lord now controls its own worklog entries exclusively via kd tk log calls, guided by the Worklog Hygiene prompt section. This addresses codex's blocking concern that the worklog was still model-controlled transcript.
- [10:47] — The codex reviewer is right — lines 598-601 unconditionally append the lord's freeform summary to the epic worklog every cycle. The lord agent is instructed to only log durable events via `kd tk log`, but the harness itself extracts and appends the first paragraph of every response regardless. I ...
- [10:47] — Files changed:
  src/kingdom/lord_harness.py | 5 +++--
   1 file changed, 3 insertions(+), 2 deletions(-)
- [10:48] — Council review: APPROVED — awaiting king review
  [claude] APPROVED: Now let me verify there are no tests that assert on the removed worklog behaviors.The `extract_lord_summary` unit tests just test the extraction function in isolation — they don't assert it's written to the worklog. No breakage.
  [codex] APPROVED: I’m re-reviewing the updated `ticket/0aa6` worktree now, focused on the remaining question from the first pass: whether the harness still writes any per-cycle lord summary into the epic worklog, or whether that path is now truly logger-only.
