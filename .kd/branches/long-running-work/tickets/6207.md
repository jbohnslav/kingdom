---
id: "6207"
status: closed
deps: []
links: []
created: 2026-03-06T12:26:58Z
type: bug
priority: 2
assignee: peasant-6207
---
# Fix --ready filter to exclude custom statuses like blocked

The --ready filter only excludes in_review and closed. With arbitrary statuses via kd tk status (e.g. blocked, waiting), those tickets still appear as ready. Fix: --ready should only include tickets with status open or in_progress.

## Acceptance Criteria

- [ ] --ready only shows tickets with status open or in_progress
- [ ] Tickets with custom statuses (blocked, waiting, etc.) are excluded from --ready

## Worklog

- [08:11] — Iteration 1/50 — calling agent
  Ticket: Fix --ready filter to exclude custom statuses like blocked
- 2026-03-06 08:12 — Fixed filter_tickets_by_deps in ticket.py:586 — changed from blocklist (not in in_review/closed) to allowlist (in open/in_progress). Added test_ready_excludes_custom_statuses. All 1669 tests pass.
- [08:13] — The bug is on line 586 of `src/kingdom/ticket.py`. The `--ready` filter uses a blocklist (`not in ("in_review", "closed")`) instead of an allowlist (`in ("open", "in_progress")`). The `kd status` command at line 441 already does it correctly with an allowlist. Let me fix the filter function and c...
- [08:13] — Files changed:
  src/kingdom/ticket.py |  4 ++--
   tests/test_ticket.py  | 15 +++++++++++++++
   2 files changed, 17 insertions(+), 2 deletions(-)
- [08:26] — Council review: APPROVED — awaiting king review
  [claude] APPROVED: The diff is empty — the commit `70f3025` referenced in the worklog isn't on this branch or has been rebased away. Let me check if it exists.The commit is on a peasant worktree branch `ticket/6207`. Let me review the actual diff and the changed files.Now let me read the current state of the file to make sure the worktree version matches what's on the branch:The base branch still has the old code (blocklist), confirming the fix lives only on `ticket/6207`. Let me now run the tests to ...
