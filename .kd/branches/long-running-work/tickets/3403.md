---
id: "3403"
status: in_progress
deps: []
links: []
created: 2026-03-06T14:17:32Z
type: task
priority: 2
assignee: peasant-3403
---
# Support Codex reasoning effort in kd config

Add first-class config support for Codex reasoning effort so kd can express settings like low/medium/high without encoding effort into the model name. Current failure mode: .kd/config.json set codex model to gpt-5.4-high, which is rejected for ChatGPT-backed Codex accounts, while gpt-5.4 works when paired with model_reasoning_effort set separately.

Decisions:
- `reasoning_effort` should be a first-class field on `agents.<name>`, separate from `model`.
- `reasoning_effort` should be supported for the `codex` backend and translated by the Codex command builder to the correct CLI/config setting.
- Setting `reasoning_effort` on unsupported backends should be an invalid config surfaced by validation and `kd doctor`.
- Different effort levels for council vs peasant should be configured via different named agents, reusing existing `council.members` and `peasant.agent` selection points rather than adding a new per-context override system in v1.

## Acceptance Criteria

- [ ] `kd` config supports `agents.<name>.reasoning_effort` with validated values for the Codex backend.
- [ ] Codex command building passes `reasoning_effort` through using the correct Codex config mechanism while keeping `model` separate.
- [ ] Using `reasoning_effort` on unsupported backends fails config validation and is reported by `kd doctor`.
- [ ] Existing configs without `reasoning_effort` continue to behave the same as before.
- [ ] Tests cover config validation, command building, and the invalid-backend case.
- [ ] Documentation or examples show the intended pattern for separate council and peasant agent names when different effort levels are needed.

## Worklog

- 2026-03-06 09:25 — Council consultation on implementation strategy for Codex reasoning effort. Consensus: do not encode effort into model names and do not rely on extra_flags as the primary API. Recommended design is a first-class agents.<name>.reasoning_effort field in kd config, with backend-specific translation in the Codex command builder to model_reasoning_effort via -c. Both responses recommend keeping per-context behavior at the agent-selection layer instead of adding a new override lattice: use separate named agents (for example codex_council vs codex_peasant) and wire them through existing council.members and peasant.agent config. Migration should be additive: absent field preserves current behavior; update docs/examples to prefer model=gpt-5.4 plus reasoning_effort; optionally warn on legacy alias model names like gpt-5.4-high. Claude suggested ignoring reasoning_effort on unsupported backends for simplicity; Codex suggested rejecting it on non-Codex backends with a clear validation error. Main open choice is lenient vs strict handling on unsupported backends.
- 2026-03-06 09:27 — Decision: make reasoning_effort a first-class agent config field and treat unsupported backends as invalid config. This should surface in validation and kd doctor instead of being silently ignored. Decision: use separate agent names for different effort profiles (for example council vs peasant) rather than adding a new per-context override lattice in v1. Rationale: this matches the current architecture, keeps model and effort separate, preserves explicit config, and reuses existing council.members / peasant.agent selection points.
- [09:29] — Iteration 1/50 — calling agent
  Ticket: Support Codex reasoning effort in kd config
