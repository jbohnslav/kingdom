---
id: "57e2"
status: closed
deps: [d6ce]
links: []
created: 2026-03-07T17:30:31Z
type: task
priority: 2
assignee: peasant-57e2
parent: 4d4a
---
# Redundant discover_epic_children calls in lord prompt building

build_lord_prompt calls discover_epic_children once directly, then each of get_startable_children, get_completed_peasants, get_active_peasants calls it again internally — 4+ redundant directory scans and ticket re-reads per cycle. The helpers also re-read the same tickets that build_lord_prompt already parsed. Refactor to discover children once and pass the list through, and consider caching the parsed ticket data across helpers.

## Acceptance Criteria

- [ ] discover_epic_children is called once per lord cycle, not per-helper
- [ ] Parsed ticket data is reused across prompt building, not re-read from disk

## Worklog

- [13:32] — Iteration 1/50 — calling agent
  Ticket: Redundant discover_epic_children calls in lord prompt building
- 2026-03-07 13:37 — Refactored lord_harness.py: all helpers (get_startable_children, get_completed_peasants, get_active_peasants, all_children_closed, has_actionable_work, get_children_summary) now accept optional children/tickets kwargs. build_lord_prompt discovers children once and parses tickets once into a dict, passing both through. Eliminated 4+ redundant discover_epic_children calls and ticket re-reads per prompt build. All 1806 tests pass.
- [13:38] — I've read the code and understand the problem. In `build_lord_prompt`, `discover_epic_children` is called once directly (line 237), then again inside `get_startable_children` (line 82), `get_completed_peasants` (line 145), and `get_active_peasants` (line 126) — 4 total calls. Each helper also re-...
- [13:38] — Files changed:
  src/kingdom/lord_harness.py | 117 ++++++++++++++++++++++++++++++++------------
   1 file changed, 87 insertions(+), 30 deletions(-)
- [13:38] — Council review: BLOCKING (bounce 1/3) — returning to working
  [claude] APPROVED: Now let me check how the loop calls these functions — specifically `all_children_closed`, `get_children_summary`, and `has_actionable_work` in `run_lord_loop`.The loop calls at lines 550, 557, 567 don't pass `children`/`tickets` kwargs. That's expected — the loop calls are separate from the prompt-building path and read fresh state each cycle. The optimization targets `build_lord_prompt` where all helpers ran against the same point-in-time data.
  [codex] BLOCKING: Reviewing the `57e2` refactor in the ticket worktree. I’m checking whether the helper signature changes really preserve behavior while cutting the redundant child discovery and ticket reads the ticket calls out.
- [13:38] — Iteration 2/50 — calling agent
  Bouncing on council feedback (1 blocking, 1 approved) — see review above
- 2026-03-07 13:41 — Addressed council feedback: run_lord_loop now discovers children and parses tickets once at the top of each cycle iteration, passing them through to all_children_closed, get_children_summary, has_actionable_work, and build_lord_prompt. discover_epic_children is now called exactly once per lord cycle. Updated 3 test mock functions to accept new kwargs. All 1806 tests pass.
- [13:41] — The council is right — the loop in `run_lord_loop` calls `all_children_closed`, `get_children_summary`, and `has_actionable_work` without sharing children/tickets, so `discover_epic_children` still fires multiple times per cycle. I need to discover children and parse tickets once at the top of ea...
- [13:41] — Files changed:
  src/kingdom/lord_harness.py | 24 ++++++++++++++++++------
   tests/test_lord.py          |  6 +++---
   2 files changed, 21 insertions(+), 9 deletions(-)
- [13:41] — Council review: APPROVED — awaiting king review
  [claude] APPROVED: Clean iteration. The key addition in commit 2 is lines 555-556 in `run_lord_loop`:
  [codex] APPROVED: Re-reviewing the updated `57e2` patch now that the loop was changed as well. I’m focusing on whether children and parsed tickets are actually shared across the whole cycle and whether the new kwargs plumbing introduced any stale-state or behavior regressions.
