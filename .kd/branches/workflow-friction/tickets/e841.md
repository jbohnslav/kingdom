---
id: "e841"
status: closed
deps: []
links: []
created: 2026-02-22T18:14:57Z
type: task
priority: 1
---
# PR #28 review feedback from Claude CI

Claude's automated PR review on #28 surfaced these issues:

## Bugs

1. **Dead ternary in cmd_copy** (app.py:861) — Both branches identical: `f" from {last.from_}" if member_filter else f" from {last.from_}"`. Already captured in 58ba item 1.

## Improvements

2. **render_ticket_panel N×collect_all_tickets on show --all** — Each call does a full filesystem walk. Hoist the collection out of the loop and pass as parameter. (58ba item 8 noted this too, but Claude gives the concrete fix: add optional `all_tickets` param.)

3. **council_review has no diff size guard** — Large diffs can silently exceed context windows. Add a warning when diff exceeds ~50k chars.

4. **ticket_unlink shares one try block for both lookups** — If `target_id` is ambiguous, the error message is misleading because both lookups are in one handler. Use separate try blocks.

## Style

5. **ticket_blocked: `in ("closed",)` → `== "closed"`** — Single-element tuple check should just be equality.

## Overlap with 58ba

Items 1 and 2 are already tracked in backlog ticket 58ba. Items 3-5 are new.

## Worklog

- Fixed dead ternary in cmd_copy (now "last response" vs "from {member}")
- Added `all_tickets` param to render_ticket_panel, hoisted collection in show --all loop
- council_review replaced raw diff with file stats (90d8) — diff size guard no longer needed
- Separated try blocks in ticket_unlink
- Fixed `in ("closed",)` → `== "closed"`
- Fixed CI: council review tests now configure git user.name/email
- Full suite: 1275 passed
