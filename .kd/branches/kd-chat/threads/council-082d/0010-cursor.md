---
from: cursor
to: king
timestamp: 2026-02-16T13:37:39Z
---

We use `pytest` for testing. It's configured via `pytest.ini` to look in the `tests` directory and add `src` to the python path, and it is listed as a development dependency in `pyproject.toml`.
