---
id: "3590"
status: closed
deps: []
links: []
created: 2026-08-21T12:42:19Z
type: task
priority: 2
closed_at: 2026-08-21T12:47:03Z
resolution: completed
closed_context: codex:a775e0448cdb5a76
assignee: codex:a775e0448cdb5a76
---
# Fix GitHub Actions CI environment isolation

## Acceptance Criteria

- [x] The four failures from GitHub Actions run 32307043372 reproduce under equivalent ambient CI settings.
- [x] CLI help assertions remain stable when Rich emits ANSI styling.
- [x] tests that exercise execution-context-aware commands isolate themselves from GitHub Actions host variables.
- [x] focused regressions, the full test suite, pre-commit, and isolated smoke workflow pass.
- [x] closing the ticket leaves `uv run kd status --check` ready.
- [x] the repair is ready to commit and update draft PR #54.

## Worklog

- 2026-08-21 — GitHub Actions run 32307043372 collected 2,218 tests and failed four while the same snapshot passed locally: two help-string assertions saw ANSI escape sequences inside `--check`/`--start`; a top-level `status` test exited 1; and a ticket dependency test's `start` command left the ticket open. PR #54 is mergeable and its Claude review passed, so this ticket is scoped to CI/test environment isolation rather than product behavior or `.kd` cleanup.
- [2026-08-21 08:44] [codex:a775e044] — Red-first reproduction confirmed both CI failure classes locally before editing. With all terminal/context variables removed, the development-source/status and dependency-transition slice produced 1 pass and 1 failure: `ticket start` could not transition without an explicit execution context. With `CI=true GITHUB_ACTIONS=true TERM=xterm-256color`, both help contract tests failed because Rich inserted ANSI sequences into `--check` and `--start`. This matches all four failures from Actions run 32307043372.
- [2026-08-21 08:45] [codex:a775e044] — Implemented a test-only hermeticity fix across four files. Semantic help assertions now remove ANSI styling before checking command text; the development-source callback test runs `status` against its own initialized `cli_project`; and the dependency transition test supplies a deterministic `KD_CONTEXT` and asserts that `ticket start` succeeds. Both exact red-first reproductions now pass (2/2 without terminal context; 2/2 with GitHub Actions ANSI conditions). Focused Ruff check/format and `git diff --check` pass.
- [2026-08-21 08:47] [codex:a775e044] — Final local verification passed: `uv run pytest` completed with 2,179 passed, 38 skipped, and 1 expected xfail; `uv run pre-commit run --all-files` passed every hook; and `bash scripts/smoke.sh` completed the isolated public workflow with readiness ready. No production behavior changed; the diff is limited to four test files plus this durable ticket.
- [2026-08-21 08:47] [codex:a775e044] — Closed the ticket and confirmed `uv run kd status --check` reports 0 open/in-progress/review tickets and `Readiness: ready` for `codex/consolidate-lifecycle`.

## Lifecycle

- 2026-08-21T12:47:03Z [codex:a775e0448cdb5a76] — closed (completed)
