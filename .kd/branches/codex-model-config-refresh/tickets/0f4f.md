---
id: "0f4f"
status: closed
deps: []
links: []
created: 2026-07-10T16:48:16Z
type: task
priority: 2
closed_at: 2026-07-10T16:52:12Z
assignee: hand
---
# Bump Kingdom release version

## Description

Bump the package from 0.6.0 to 0.6.1 for the model-role configuration release,
as directed by the King.

## Acceptance Criteria

- [x] `pyproject.toml` and the root `kingdom-cli` package in `uv.lock` report 0.6.1.
- [x] The installed package metadata reports 0.6.1.
- [x] Lockfile validation and the full test suite pass.

## Worklog

- 2026-07-10: Verified the corrected 0.6.1 release: `uv lock --check` passed,
  installed package metadata reported 0.6.1, `git diff --check` passed, and the
  full suite completed with 1943 passed, 38 skipped, and 1 expected failure.

- 2026-07-10: King corrected the release target from 0.7.0 to 0.6.1. Reopened
  the ticket and updated both authoritative version sources; verification is
  pending.

- 2026-07-10: Updated `pyproject.toml` and the editable root package entry in
  `uv.lock` to 0.7.0. `uv lock --check` passed, installed package metadata
  reported 0.7.0, `git diff --check` passed, and the full suite completed with
  1943 passed, 38 skipped, and 1 expected failure.

- 2026-07-10: Current release and latest tag are 0.6.0. Selected 0.7.0 as the
  next semantic version because the model-role work adds backward-compatible
  configuration features rather than only a bug fix.
