---
id: "7e6e"
status: closed
deps: []
links: []
created: 2026-02-17T03:18:15Z
type: task
priority: 1
---
# Fix pre-existing test failure: test_cursor_assistant_snapshot_chunks_should_use_latest_snapshot

Found on `polish` branch during 8dea research. The test `TestThreadPollerStreaming::test_cursor_assistant_snapshot_chunks_should_use_latest_snapshot` fails with `assert 0 == 1` (no StreamDelta events emitted). Likely caused by changes to cursor snapshot handling on this branch.

Location: `tests/test_tui_poll.py:233`

## Acceptance Criteria

- [x] Test passes
- [x] Cursor snapshot merge logic is correct for cross-batch cumulative snapshots

## Worklog

The test `test_cursor_assistant_snapshot_chunks_should_use_latest_snapshot` only
existed on the `polish` branch — it never made it to master. On master, all 60
poll tests pass, and existing tests already cover cursor snapshot behavior:
- `test_cursor_snapshot_fresh_after_finalization` (reset between rounds)
- `test_cursor_non_cumulative_fragments_concatenated` (within-batch concat)
- `test_cursor_cross_batch_fragments` (across-batch concat)

No fix needed — closing as already covered.
