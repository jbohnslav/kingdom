---
id: "ee01"
status: closed
deps: []
links: []
created: 2026-02-17T18:31:13Z
type: task
priority: 1
---
# `kd init` should install the Claude agent skill

## Context

The kingdom agent skill (`SKILL.md`) lives in the repo and is symlinked to `~/.claude/skills/kingdom` for development. Users who install from PyPI have no way to get the skill without cloning the repo.

## Plan

1. Bundle `SKILL.md` inside the Python package (`src/kingdom/skills/SKILL.md`)
2. During `kd init`, copy the bundled skill to `~/.claude/skills/kingdom/SKILL.md` using `importlib.resources`
3. Keep the repo symlink for development; PyPI users get it via `kd init`

## Acceptance Criteria

- [x] `SKILL.md` is included in the pip package as package data
- [x] `kd init` installs the skill to `~/.claude/skills/kingdom/`
- [x] Works for fresh `pip install kingdom-cli && kd init` without cloning the repo

## Worklog

- Copied skill files into `src/kingdom/skill/` (SKILL.md + references/*.md)
- Added `[tool.setuptools.package-data]` to pyproject.toml for `.md` files
- Added `install_skill()` to cli.py, called from `kd init`
- Skips install when target is a symlink (preserves dev setup)
- Uses `importlib.resources` to read bundled files
- 4 tests: copies files, skips symlink, idempotent, integrated with `kd init`
- Manual e2e test with `HOME=/tmp/... kd init` confirmed files land correctly
