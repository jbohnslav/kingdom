---
id: "4784"
status: open
deps: []
links: [aa61]
created: 2026-02-27T11:55:33Z
type: task
priority: 2
---
# Fix known data-layer bugs (R8)

## Problem

Several known bugs exist in the data layer that haven't been fixed yet.

## Acceptance Criteria

- [ ] **Priority-0-as-falsy**: `parse_ticket` uses `if priority is not None:` instead of `if priority:`
- [ ] **`find_ticket` ambiguity**: Prefer current branch → backlog → archive before raising `AmbiguousTicketMatch`; return a `TicketMatch` with location info
- [ ] **`collect_all_tickets` dedup**: Deduplicate by ticket ID, preferring branch copy over backlog/archive
- [ ] **Deps/links uniqueness**: Normalize to ordered sets after parsing
- [ ] Each fix has a corresponding test
- [ ] Existing ticket `aa61` (find_ticket duplicates) is closed by this work
- [ ] All tests pass
