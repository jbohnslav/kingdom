---
id: "4658"
status: closed
deps: []
links: []
created: 2026-03-01T18:59:30Z
type: bug
priority: 1
closed_at: 2026-03-02T16:08:14Z
---
# Peasant agent escapes worktree and commits to wrong branch

## The Problem

In the 8364 run, the peasant agent was launched with `cwd=.kd/worktrees/8364` (on branch `ticket/8364`), but navigated to the main repo at `/Users/jrb/code/kingdom` and committed `1e29a7d` to `code-cleanup` instead. The harness correctly rejected the DONE claim (no changes in the worktree), but the rogue commit was already on the King's working branch. This wasted 7 minutes and polluted `code-cleanup`.

There are **zero guardrails** preventing this:

1. **No prompt boundary.** The system prompt in `build_prompt()` (`harness.py:33-86`) never mentions the worktree path, the ticket branch name, or that the agent should stay in its working directory.
2. **No runtime detection.** Nothing checks whether the agent `cd`'d out of the worktree during execution.
3. **No post-execution branch validation.** `has_code_changes()` (`harness.py:285-316`) checks the worktree for changes but doesn't verify commits are on the correct branch (`ticket/{id}`).
4. **No worktree-specific CLAUDE.md.** The worktree's `CLAUDE.md` is identical to the root — no isolation instructions.

## Root Cause

The agent sees file paths like `src/kingdom/harness.py` and resolves them against the filesystem. If it runs `git rev-parse --show-toplevel` or just navigates up, it finds `/Users/jrb/code/kingdom` (the main repo) and starts working there instead of `.kd/worktrees/8364`. Nothing in the prompt anchors it.

## Implemented Fix (rescoped)

Layers 3 (parent-branch audit) and 4 (worktree CLAUDE.md) were dropped as over-engineering.
Auditing the parent branch is fragile on a busy feature branch with multiple peasants
merging, and the worktree CLAUDE.md duplicates the prompt anchoring already in place.

### Layer 1: Prompt anchoring (prevent) — DONE
- `build_prompt()` tells the agent its worktree path, branch, and forbids leaving.

### Layer 2: Branch validation (detect) — DONE
- `check_worktree_branch()` runs `git rev-parse --abbrev-ref HEAD` in the worktree
  and compares against the expected branch (`ticket/{id}` or feature branch in hand mode).
- Pre-loop check: aborts before calling the agent if the worktree is already wrong.
- Post-iteration check: aborts after each agent call if the branch changed.
- Both log BRANCH ESCAPE to the worklog and return status "failed".

### Layer 5: Session state seeded before worker launch — DONE
- `peasant start` now writes session state (status, ticket, hand_mode, agent, etc.)
  before spawning the worker process, so the worker sees correct state on first read.
- Post-launch update only writes `pid`, so a fast worker failure isn't clobbered
  back to "working".
- Launch exceptions caught and session set to "failed" instead of stuck "working".

## Acceptance Criteria

- [x] System prompt tells the agent its worktree path and branch name
- [x] System prompt explicitly says to stay in the worktree
- [x] After each iteration, harness validates the worktree branch hasn't changed
- [x] Branch escape triggers a loud warning in the worklog and rejects the iteration
- [x] Test: branch escape is detected and rejected (7 tests)
- [x] Session state seeded before worker launch (hand_mode visible on first read)
- [x] Post-launch update only writes pid (fast worker failure not clobbered)
- [x] Launch failure sets session to "failed" (no phantom "working" sessions)
- [x] Tests: 3 CLI-level tests for the race fix

## Worklog

- [2026-03-01] Discovered during 8364 autopsy. Agent committed 1e29a7d to code-cleanup from worktree that should have been on ticket/8364. Promoted from backlog to P1.
- 2026-03-01 15:58 — First-pass peasant prompt rewrite landed in harness: added explicit worktree path/branch anchoring, parent-repo stay-out rule, repo-instructions hook (AGENTS.md/CLAUDE.md), concrete first-paragraph contract for kd peasant watch, and explicit worklog update guidance via kd tk log or direct ticket edits. Targeted harness tests and ruff passed.
- 2026-03-02 — Rescoped: dropped Layers 3 (parent-branch audit) and 4 (worktree CLAUDE.md). Added `check_worktree_branch()` in harness.py with pre-loop and post-iteration checks. 7 new tests (5 unit for the function, 2 integration for the loop). All 1521 tests pass.
- 2026-03-02 — Codex review found two bugs: (1) pre-loop failure left session stuck in "working" because early return skipped update_agent_state, (2) start_sha recorded before branch check could poison baseline. Fixed by swapping order and adding state update before early return.
- 2026-03-02 — Codex review found race in peasant start: worker reads session state (hand_mode) before parent writes it. Fixed by seeding session state before launch, post-launch only writes pid. Added launch failure catch to avoid phantom "working" sessions. 3 new CLI-level tests. All 1524 tests pass.
