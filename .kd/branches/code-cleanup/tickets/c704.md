---
id: "c704"
status: closed
deps: [ffc8]
links: []
created: 2026-02-27T11:55:36Z
type: task
priority: 2
assignee: peasant-c704
---
# Consolidate test fixtures and reorganize tests (R10)

## Problem

Test files mirror the monolithic CLI structure. The `project` fixture is duplicated 8+ times. After the CLI split, tests should be reorganized to match the new module structure.

## Implementation Plan

### Phase 1: Fixture consolidation

Move the duplicated `project` fixture into `tests/conftest.py`. Seven files have
near-identical copies (`test_session.py`, `test_thread.py`, `test_tui_reply.py`,
`test_harness.py`, `test_tui.py`, `test_tui_integration.py`, `test_council.py`).

- Add a `branch_name` fixture to `tests/conftest.py` that reads
  `request.module.BRANCH` when the module defines one (files keep their own
  `BRANCH` constants — many tests reference the branch name in path assertions).
- Add a base `project` fixture that does
  `ensure_branch_layout(tmp_path, branch_name)` and returns `tmp_path`.
- Add a `project_with_run` variant that also calls `set_current_run`
  (needed by `test_harness.py` and the split ticket test files).
- Remove the per-file fixture copies from the 7 duplicating files.
- The class-level `project` override in `TestMentionBumpAutoTurns`
  (`test_tui.py`) stays as-is — it does `ensure_base_layout` +
  `ensure_branch_layout` + `set_current_run`, which is genuinely different.
- Also lift `setup_project()` / `CliRunner()` boilerplate from CLI test files
  into shared fixtures if it helps the subsequent split.

Run full test suite. Commit.

### Phase 2: Split `test_cli_ticket.py`

The file is 3,194 lines with 32 test classes. Split into:

| New file | Classes |
|---|---|
| `test_cli_ticket_lifecycle.py` | `TestTicketCreate`, `TestTicketCreateOptions`, `TestTicketCloseArchive`, `TestTicketCloseIdempotent`, `TestTicketCloseReason`, `TestTicketCloseDuplicate`, `TestTicketCloseDuplicateValidation`, `TestTicketCloseUnblocked`, `TestTicketClosed`, `TestTicketDelete`, `TestTicketMove`, `TestTicketPull`, `TestTicketAddNote` |
| `test_cli_ticket_list.py` | `TestTicketList`, `TestTicketListClosedCount`, `TestTicketListPriority`, `TestTicketListTable`, `TestTicketListJson`, `TestTicketListFilters`, `TestTicketListDepsJson`, `TestNoResultsMessages`, `TestFormatTicketLine`, `TestFormatTicketSummary` |
| `test_cli_ticket_show.py` | `TestTicketShow`, `TestTicketShowRelationships` |
| `test_cli_ticket_deps.py` | `TestTicketDep`, `TestTicketUndep`, `TestTicketDepTree`, `TestTicketDepCycle`, `TestTicketBlocked` |
| `test_cli_ticket_links.py` | `TestTicketLink` |
| `test_cli_ticket_worklog.py` | `TestTicketLog` |

Keep `test_cli_ticket.py` deleted (no stub). Use shared fixtures by default;
keep explicit inline setup only where the test is verifying alternative
branch/run state (e.g., `test_move_no_active_branch_errors`, done-branch
dependency/list cases).

Run full test suite. Commit.

### Phase 3: Import sweep

Update test files to import from the specific CLI submodules
(`kingdom.cli.ticket`, `kingdom.cli.council`, `kingdom.cli.peasant`, etc.)
instead of `from kingdom.cli import ...` or `from kingdom import cli`.

Keep one thin smoke test around `kingdom.cli.__init__` so the top-level wiring
still gets coverage (Codex's suggestion — good catch).

Run full test suite. Commit.

## Acceptance Criteria

- [ ] `project` fixture consolidated into `tests/conftest.py` (base + `project_with_run` variant)
- [ ] `setup_project()` / `CliRunner()` boilerplate lifted into shared fixtures where it reduces duplication
- [ ] `test_cli_ticket.py` split into lifecycle, list, show, deps, links, worklog
- [ ] Tests import from `kingdom.cli.ticket`, `kingdom.cli.council`, etc. instead of the monolith
- [ ] Top-level `kingdom.cli` wiring still has smoke-test coverage
- [ ] All tests pass after each phase

## Worklog

- [13:12] — Iteration 1/50 — calling agent
  Ticket: Consolidate test fixtures and reorganize tests (R10)
- 2026-03-02 13:21 — Phase 1 done — fixtures consolidated in conftest.py. Starting Phase 2: splitting test_cli_ticket.py into 6 files with cli_project fixture conversion.
- [13:27] — Backend call timed out
- [16:51] — Iteration 1/50 — calling agent
  Ticket: Consolidate test fixtures and reorganize tests (R10)
- [17:06] — Backend call timed out
- [22:14] — Iteration 1/50 — calling agent
  Ticket: Consolidate test fixtures and reorganize tests (R10)
- 2026-03-02 22:32 — Phase 3 done — import sweep complete. All test files now import from specific CLI submodules (kingdom.cli.ticket, kingdom.cli.council, kingdom.cli.peasant, kingdom.cli.config, kingdom.cli.helpers). Added TestCliWiring smoke tests to test_cli.py for top-level kingdom.cli wiring coverage. All 1525 tests pass.
- [22:32] — Let me read the ticket and understand the current state of the codebase.Good - Phase 1 and Phase 2 are committed. There are unstaged changes in several test files. Let me understand what state those are in.The unstaged changes are Phase 3 (import sweep) - a previous iteration started this but tim...
- [22:32] — Files changed:
  tests/test_cli.py                  | 122 +++++++++++++--------
   tests/test_cli_council.py          | 215 +++++++++++++++++++------------------
   tests/test_cli_design_breakdown.py |  10 +-
   tests/test_cli_peasant.py          | 166 ++++++++++++++--------------
   tests/test_cli_status.py           |  16 +--
   tests/test_cli_ticket_current.py   |  20 ++--
   tests/test_cli_ticket_deps.py      |  50 ++++-----
   tests/test_cli_ticket_lifecycle.py | 124 ++++++++++-----------
   tests/test_cli_ticket_links.py     |  12 +--
   tests/test_cli_ticket_list.py      | 130 +++++++++++-----------
   tests/test_cli_ticket_show.py      |  30 +++---
   tests/test_cli_ticket_worklog.py   |  14 +--
   tests/test_done.py                 |  30 +++---
   tests/test_init.py                 |  42 ++++----
   uv.lock                            |   2 +-
   15 files changed, 507 insertions(+), 476 deletions(-)
- [22:35] — Council review: BLOCKING (bounce 1/3) — returning to working
  [codex] BLOCKING: Using the `kingdom` skill for this review because this is a `kd` workflow/test refactor ticket. I’m reviewing the three commits as a unit, then I’ll run `ruff` and the full test suite on that exact snapshot to check for regressions rather than relying on the current worktree.
  [claude] APPROVED: This is a large refactoring ticket (24 files, ~3500 lines moved). Let me review it systematically by phase.Three commits matching the three phases. Let me start with Phase 1 (fixture consolidation).Good structure. Let me review Phase 1 — the conftest.py fixture consolidation.Phase 1 looks clean. The `branch_name` fixture dynamically reads `BRANCH` from each test module, so existing modules keep their own branch constants. The `project_with_run` variant adds `set_current_run()`. The ...
- [22:35] — Iteration 2/50 — calling agent
  Bouncing on council feedback (1 blocking, 1 approved) — see review above
- 2026-03-02 23:10 — Council bounce: Codex blocked on Phase 3 — test files still import top-level app from kingdom.cli instead of submodule apps. Need to switch council/peasant/ticket/design tests to invoke submodule apps directly.
- 2026-03-02 23:19 — Council bounce addressed — all command-specific tests now invoke submodule apps (ticket_app, council_app, peasant_app, design_app) directly instead of routing through the top-level app. Only test_cli.py, test_done.py, test_cli_status.py, and test_init.py still use the top-level app, which is correct since they test commands defined in kingdom.cli.__init__. All 1525 tests pass.
- [23:19] — The council blocked on Phase 3 being incomplete — test files like `test_cli_council.py`, `test_cli_peasant.py`, and the ticket test files still invoke the top-level `app` from `kingdom.cli` instead of using the submodule apps (`council_app`, `ticket_app`, `peasant_app`) directly. I need to change...
- [23:19] — Files changed:
  tests/test_cli_council.py          | 208 ++++++++++++++++++-------------------
   tests/test_cli_design_breakdown.py |  10 +-
   tests/test_cli_peasant.py          | 168 +++++++++++++++---------------
   tests/test_cli_ticket_current.py   |  20 ++--
   tests/test_cli_ticket_deps.py      |  50 ++++-----
   tests/test_cli_ticket_lifecycle.py | 131 ++++++++++++-----------
   tests/test_cli_ticket_links.py     |  12 +--
   tests/test_cli_ticket_list.py      | 115 ++++++++++----------
   tests/test_cli_ticket_show.py      |  30 +++---
   tests/test_cli_ticket_worklog.py   |  14 +--
   tests/test_tui.py                  |  46 ++++----
   11 files changed, 401 insertions(+), 403 deletions(-)
- [23:22] — Council review: APPROVED — awaiting king review
  [codex] APPROVED: Using the `kingdom` skill for this review because this is a `kd` workflow/test refactor ticket. I’m reviewing the updated four-commit stack as a unit, then I’ll verify the exact snapshot with `ruff` and the full test suite before judging whether Phase 3 is now actually complete.
  [claude] APPROVED: This is round 2, with a new commit `73f1f7a` addressing the codex blocking feedback. Let me review the new commit and then verify the full state.The new commit changes 11 files. The core change is switching from the top-level `app` to submodule-specific apps. Let me examine a few representative files.The pattern is clear and consistent: `from kingdom.cli import app` → `from kingdom.cli.ticket import ticket_app`, and invocations change from `["tk", "create", ...]` → `["create", ...]`...
