---
id: "7868"
status: closed
deps: [5afc]
links: []
created: 2026-02-26T16:22:55Z
type: task
priority: 2
assignee: peasant-7868
---
# kd council chat: auto-start new thread when no threads exist

`kd chat` is moving to `kd council chat` under R1 (5afc). This ticket fixes the UX where running
the chat command with no existing threads errors out instead of creating one.

## Acceptance Criteria

- [ ] `kd council chat` auto-creates a new thread when none exist for the current branch
- [ ] Existing threads still work as before (prompt to select if multiple)
- [ ] Test covers the no-threads-exist path

## Worklog

- [14:08] — Iteration 1/50 — calling agent
  Ticket: kd council chat: auto-start new thread when no threads exist
- [14:24] — Modified `kd council chat` to auto-create a new thread when none exist for the current branch, instead of printing an error message asking the user to run `--new`.
- [14:24] — Files changed:
  src/kingdom/cli/council.py |  9 +++++++--
   tests/test_tui.py          | 12 ++++++++++--
   2 files changed, 17 insertions(+), 4 deletions(-)
- [14:26] — Council review: APPROVED — awaiting king review
  [codex] APPROVED: Using the `kingdom` skill for this review because this is a `kd council` workflow change. I’m checking the current implementation in the touched files, then I’ll run the relevant tests plus the full suite and `ruff` to verify the branch state.
  [claude] APPROVED: Let me read the full context around the changed code and run the test suite and linter.Let me verify whether the diff has been applied by checking the test file.The diff hasn't been applied yet — I'm reviewing against the current source. Let me check the imports and the `--new` thread creation path for comparison.Let me check the import for `set_current_thread` in the function.Good — `set_current_thread` is module-level imported (line 24), `secrets` is imported at line 7, and `creat...
