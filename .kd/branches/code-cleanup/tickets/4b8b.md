---
id: "4b8b"
status: open
deps: [5afc]
links: []
created: 2026-02-27T11:55:21Z
type: task
priority: 1
---
# Consolidate duplicated patterns (R5)

## Problem

Several patterns are duplicated across the codebase: ticket resolution try/except blocks (~10 copies), inline ISO datetime parsing (3+ copies), and hand-built YAML frontmatter in multiple files. Consolidating these before the CLI split makes the split cleaner.

## Acceptance Criteria

- [ ] `resolve_ticket_or_exit(base, ticket_id)` helper in `cli/helpers.py` replaces all ~10 identical `try: find_ticket() / except AmbiguousTicketMatch` blocks
- [ ] `parse_iso_datetime(s)` utility in `parsing.py` replaces all inline `"Z"` → `"+00:00"` conversions
- [ ] `serialize_frontmatter(fields)` shared helper in `parsing.py` unifies hand-built YAML frontmatter in `ticket.py` and `thread.py`
- [ ] No remaining duplicated patterns for these three cases
- [ ] All tests pass
