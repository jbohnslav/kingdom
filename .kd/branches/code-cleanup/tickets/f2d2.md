---
id: "f2d2"
status: closed
deps: []
links: []
created: 2026-02-27T13:20:02Z
type: task
priority: 2
assignee: peasant-f2d2
---
# Add --watch flag to kd peasant start

## Context

After `kd peasant start`, the King almost always wants to watch progress.
Today that's two commands: `kd peasant start <id>` then `kd peasant watch <id>`.
A `--watch` flag on start collapses this into one step.

We intentionally do NOT auto-watch by default. Coding agents run
`kd peasant start` and expect it to return immediately. A blocking watch
loop would cause timeouts or accidental Ctrl+C kills. The default stays
non-blocking; `--watch` is a human convenience flag.

## Implementation

Add a `--watch` / `-w` flag to `peasant_start()` (and `peasant_start_hand()`
if it exists). When set, after the peasant process is spawned successfully,
call `peasant_watch()` inline (or the equivalent watch loop) instead of
returning.

The flag should come after the existing options. Default is False.

## Acceptance Criteria

- [ ] `kd peasant start <id> --watch` spawns the peasant then enters watch mode
- [ ] `kd peasant start <id>` (no flag) behaves exactly as today — returns immediately
- [ ] `--watch` / `-w` flag works on both `peasant start` and `peasant start --hand`
- [ ] Watch mode exits cleanly on Ctrl+C without killing the peasant process
- [ ] Tests cover: start with --watch calls watch, start without --watch does not
- [ ] No TTY detection or auto-watch logic — the flag is explicit

## Worklog

- [09:16] — Iteration 1/50 — calling agent
  Ticket: Add --watch flag to kd peasant start
- 2026-03-02 09:19 — Implemented --watch/-w flag on peasant start. Added parameter to peasant_start(), calls peasant_watch(ticket_id) after spawn when set. Three new tests: with watch, without watch, and hand+watch. Full suite green (1514 passed), ruff clean, pre-commit hooks pass.
- [09:19] — Let me read the ticket and explore the relevant source code first.Good, I have a solid understanding of the codebase. Let me read the specific files I need to modify.Now let me check the existing tests for the peasant start command.Now I have a clear picture. The implementation is straightforward...
- [09:19] — Files changed:
  src/kingdom/cli/peasant.py |  5 ++++
   tests/test_cli_peasant.py  | 64 ++++++++++++++++++++++++++++++++++++++++++++++
   2 files changed, 69 insertions(+)
- [09:21] — Council review: APPROVED — awaiting king review
  [codex] APPROVED: Using the `kingdom` skill for this review because this is a `kd` CLI ticket change. I’m reading the ticket and actual diff first, then I’ll run the targeted tests, full suite, and `ruff` before assessing correctness and regressions.
  [claude] APPROVED: Let me find the commit and review the diff.Good, clean diff. Now let me read the surrounding context to verify placement and that the `--hand` path also reaches the watch call.Good. Now let me verify that the `--hand` code path also reaches line 353. Let me check if the hand mode has an early return that would skip the watch.Both the `hand` and worktree paths converge — they both set `worktree_path` and proceed to step 4 (launch harness), then step 5 (update state), then the output ...
