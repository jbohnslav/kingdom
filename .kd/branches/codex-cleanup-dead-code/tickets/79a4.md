---
id: "79a4"
status: open
links: []
created: 2026-09-05T23:42:47Z
type: task
priority: 2
deps: []
parent: "1465"
---
# Simplify internal interfaces and remove compatibility aliases

Implement audit findings 7–9 and the redundant design-path helpers from finding 11.

Remove remaining test-only exports in kingdom.cli and imports from submodules back through the top-level CLI. Give display.py sole ownership of color policy. Replace TicketMatch tuple emulation with a small frozen dataclass exposing ticket/path/location and update all internal callers. Remove AgentDef.reasoning_effort and config/display alias handling; effort is the canonical key. Collapse get_branch_paths/get_design_paths to the paths actually used. Update tests at real implementation boundaries rather than preserving old mock targets.

The provider setting model_reasoning_effort is a real external API and remains. Useful CLI shortcuts are handled separately from Python API compatibility.

## Acceptance Criteria

- [ ] No unused re-export contracts or circular helper lookups through kingdom.cli remain; CLI entry points still work.
- [ ] TicketMatch uses named attributes, with old tuple methods and caller assumptions removed.
- [ ] effort is the only Kingdom configuration/constructor field; obsolete alias tests and source-attribution fallback are removed.
- [ ] Color and design-path ownership are simple and explicit.
- [ ] Full suite including Textual integration and Ruff pass; config/help output is manually verified.

## Worklog

- 2026-09-05 — Scoped from the King’s request for the full six-area cleanup; historical closed-ticket compatibility is not a preservation requirement.
