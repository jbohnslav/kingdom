---
id: "1465"
status: open
links: []
created: 2026-09-05T23:42:38Z
type: epic
priority: 2
deps: []
---
# Complete the full Kingdom cleanup

Finish all six areas from the broader cleanup summary. This is the second cleanup effort after completed ticket 4088, which removed abandoned code and its dedicated tests. Source evidence and original locations: docs/slop-audit-2026-09-05.md (remaining findings 7–20, plus the legacy council reader from finding 3).

The King explicitly requested the full cleanup and said not to worry about the 508 historical closed tickets. Preserving resolution-less closures or other obsolete stored formats is not an acceptance requirement. Remove compatibility branches instead of retaining them solely for those records. No cross-repository inventory, migration project, or further historical-data approval is a prerequisite. Existing historical files need not be mass-deleted or rewritten just to remove support.

Target: one current internal API, one current runtime representation, a smaller CLI surface, tests of real behavior, shared live implementations, and accurate current docs. Preserve current ticket/context/worker correctness, locking, atomic writes, ordinary terminal identity, useful aliases such as tk, and actual supported provider integrations. Removing obsolete internal APIs and CLI options is authorized.

## Children

- 79a4 — Simplify internal interfaces and configuration aliases.
- 80d2 — Retire old runtime formats and historical-data compatibility.
- aeec — Remove inert/overloaded CLI options and design-approval ceremony.
- dcab — Consolidate duplicated streaming, hook normalization, and status handling.
- 8456 — Prune low-value tests, repair vacuous checks, and enable TUI integration in CI.
- 67f9 — Finish documentation, workflow, and scaffolding cleanup.

Implementation order follows the dependencies below; each code ticket must pass the full suite before closure. Tests affected by an implementation change are updated with that change; the dedicated test ticket handles remaining suite-wide cleanup.

## Acceptance Criteria

- [ ] All six children are completed, with no audit item left silently deferred.
- [ ] Legacy closed-ticket inference and obsolete runtime support are removed without treating the 508 old tickets as a preservation gate.
- [ ] Current CLI, host, worker, and terminal workflows work with the simplified interfaces and state model.
- [ ] Full pytest suite with Textual integration, Ruff, pre-commit, and CLI smoke pass; changed CLI output is manually inspected.
- [ ] Audit progress and current documentation reflect the final implementation, and kd status --check passes before a PR.

## Worklog

- 2026-09-05 — Scoped from the King’s request for the full six-area cleanup; historical closed-ticket compatibility is not a preservation requirement.
