---
id: "8456"
status: open
links: []
created: 2026-09-05T23:42:47Z
type: task
priority: 2
deps: ["79a4", "80d2", "aeec", "dcab"]
parent: "1465"
---
# Replace low-value tests with behavior checks and enable TUI CI

Implement audit findings 16–19 after implementation tickets have removed their affected obsolete tests.

Delete README/skill prose snapshots, historical release-ticket/path assertions, and tests tied to a literal release version. Keep executable docs, artifact/mirror/schema checks, current CLI behavior, and intentional release/review-trigger policy checks. Fix quit/Escape tests so disabling exit makes them fail before test-context cleanup. Make warning tests assert warning output and failure status. Remove tests for missing .tickets files, module-existence skip escape hatches, and redundant conftest path setup. Enable Textual integration in checked-in CI and release validation. Make the remaining Cursor regression xfail strict, or fix it with evidence, so an unexpected pass is visible.

Use mutation checks on the repaired exit tests to demonstrate the original false-confidence problem is gone. Do not replace deleted snapshots with equivalent snapshots of new wording.

## Acceptance Criteria

- [ ] No tests require historical release IDs, exact prose, or a hardcoded release version; meaningful executable/packaging/policy tests remain.
- [ ] Quit and both Escape-exit tests fail with exit behavior disabled and pass with it enabled; warning tests assert their advertised outcomes.
- [ ] Old missing-ticket tests, missing-module skips, and redundant path insertion are removed.
- [ ] CI and release actually run Textual integration; remaining expected failures are explicit and strict.
- [ ] Full suite including Textual integration and repository hooks pass, with skips/xfails explained in the worklog.

## Worklog

- 2026-09-05 — Scoped from the King’s request for the full six-area cleanup; historical closed-ticket compatibility is not a preservation requirement.
