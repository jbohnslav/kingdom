---
id: "7a1d"
status: closed
deps: [7afc]
links: []
created: 2026-02-16T18:31:16Z
type: task
priority: 2
closed_at: 2026-02-25T04:44:23Z
---
# LLM-to-LLM @mentions in kd chat round-robin — parse member response text for @member patterns, reorder scheduling queue

## Acceptance Criteria

- [x] After each member auto-turn response, parse `@member` patterns using the existing mention regex (`(?<!\w)@(\w+)`)
- [x] Mention bump is applied after base round order is built (shuffle for `natural`, fixed order for `round_robin`)
- [x] Mentioned valid members move to the front; non-mentioned members keep relative order
- [x] Unknown names and `@king` are ignored
- [x] Mention bump applies only in `natural` and `round_robin` modes
- [x] Mention bump does not create extra turns; all turns still count inside the current round cap
- [x] Tests cover single mention, multiple mentions, duplicate mentions, invalid mentions, and no-mention behavior

## Worklog

- Added `mention_bump()` helper: parses @mentions from response text, reorders remaining queue with mentioned members first
- Modified `run_query()` to return response body text (or None on error/stale)
- Updated `sequential_auto_turns()` to use dynamic queue with mention bump after each response
- Mention bump only active in natural and round_robin modes (via `sequential_auto_turns`)
- 7 unit tests for `mention_bump()`: single, multiple, duplicate, unknown, @king, no-mention, not-in-remaining
- 1 integration test: auto-turn queue reordering when claude mentions @extra
- All 1378 tests pass
