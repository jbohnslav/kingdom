---
id: "539b"
status: closed
deps: [f122]
links: []
created: 2026-08-02T13:57:37Z
type: task
priority: 2
closed_at: 2026-08-02T18:05:36Z
assignee: codex:fcce7f4353b26c01
parent: 029e
---
# Show concurrent sessions and stale assignments in status and TUI

Make concurrent work legible instead of hiding it behind a single "current" row.

## Acceptance Criteria

- [x] `kd status` shows each live/stale session, host, role, ticket, epic, and last-seen time.
- [x] `kd tk list` can include context/assignee without making the default table unreadable.
- [x] The retained TUI exposes the same assignments and marks stale contexts clearly.
- [x] A cleanup/doctor action removes stale bindings without changing ticket history.
- [x] JSON output is stable enough for host plugins and external tooling.

## Worklog

- 2026-08-02 — Added failing tests for JSON and human session visibility, live vs
  stale classification, explicit stale pruning, compact context IDs in `tk list`,
  TUI branch-context injection, and a TUI `/status` command. Collection fails
  before implementation because the execution-context listing API does not exist.
- 2026-08-02 — Added role-aware context records, live/stale enumeration, explicit
  `kd status --prune-stale`, stable JSON context records, compact human context
  IDs, and session visibility in both `kd status` and the TUI `/status` command.
- 2026-08-02 — Manually verified human and JSON status output against the active
  Codex context. Dogfooding found closed legacy assignments in status; filed
  backlog ticket `14fa`. Ruff passed and the full suite passed: 1945 passed,
  38 skipped, 1 xfailed.
- [14:05] — Closed: Implemented live/stale session visibility, compact assignees, TUI status, stable JSON, and explicit stale pruning; full suite passes.
