---
id: "f240"
status: closed
deps: []
links: []
created: 2026-08-03T12:57:30Z
type: task
priority: 2
closed_at: 2026-08-03T19:15:02Z
resolution: completed
closed_context: codex:a775e0448cdb5a76
assignee: codex:0f4ec9f976ec982b
---
# Allow peasant accept when the logical kd branch differs from the checkout

## Acceptance Criteria

- [x] `kd peasant accept <ticket>` resolves the reviewed ticket branch and
  recorded Kingdom feature without requiring the checkout's Git branch name to
  equal the feature key.
- [x] Acceptance still refuses ambiguous or unsafe targets and never merges an
  unrelated ticket branch.
- [x] A deliberate logical-feature/checkout mismatch has regression coverage.
- [x] The error path offers a safe exact recovery command when automatic
  acceptance cannot proceed.

## Context

Dogfooding `92ca` on 2026-08-03 produced a council-approved commit on
`ticket/92ca`, but `kd peasant accept 92ca` refused because `.kd/current` named
`codex/workflow-polish` while the intentionally busy checkout was on `master`.
The ticket and peasant session were exact, so the branch-name equality check
turned Kingdom's logical grouping into an unnecessary Git-layout constraint.

## Worklog

- [2026-08-03 15:09] [codex:0f4ec9f9] — Red-first accept regressions failed as expected.
  A reviewed ticket/session on logical feature-peasant-test was rejected solely because the invocation checkout was master.
  Conflicting persisted session.ticket and duplicate peasant sessions across features reached subprocess logic instead of refusing safely.
  Chosen invariant: the checkout is the intentional integration target, the only merge source is derived as ticket/<full-id>, and one uniquely owning reviewed session must re-resolve that exact ticket inside its Kingdom feature before merge.
- [2026-08-03 15:13] [codex:a775e044] — Implementation completed after the red regressions. Acceptance now scans for one unique non-idle peasant session for the exact full ticket ID, re-resolves the ticket inside that owning Kingdom feature, rejects a conflicting persisted session ticket, and derives the only merge source as ticket/<full-id>. The invocation checkout is the explicit integration target, so a logical feature key may differ from the physical branch without weakening source selection. Duplicate sessions, unrelated session records, detached HEAD, review-state failures, dirty targets, and merge conflicts still refuse before closure with exact review/retry guidance. All 15 accept-focused regressions and all 160 peasant CLI tests pass; Ruff check and format check pass. Manual `kd peasant accept f240` inspection showed the no-session refusal with exact review and retry commands.
- [2026-08-03 15:15] [codex:a775e044] — Parent review passed: full suite 2175 passed, 38 skipped, 1 expected failure; whole-tree Ruff check and format check passed; all pre-commit hooks passed without rewriting files.

## Lifecycle

- 2026-08-03T19:15:02Z [codex:a775e0448cdb5a76] — closed (completed)
