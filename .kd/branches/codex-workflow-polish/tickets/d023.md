---
id: "d023"
status: closed
deps: [22fb]
links: []
created: 2026-08-02T13:57:51Z
type: task
priority: 2
closed_at: 2026-08-03T12:55:10Z
assignee: codex:3771d784a5b019f4
parent: d3a5
---
# Add Codex plugin hooks and install-update support

Package Kingdom for Codex using current lifecycle hooks and the existing repo skill,
with the same update ergonomics as the Claude integration.

## Dogfood Assignment

Implement this ticket with a native Codex subagent. The owning session must keep this
Markdown ticket current and record any intervention needed to steer or recover the
subagent.

## Acceptance Criteria

- [x] Installation configures the Kingdom skill and supported Codex hooks idempotently.
- [x] Codex session/thread IDs bind to the correct Kingdom context.
- [x] Stop, compaction, session, and subagent events use the shared adapter.
- [x] Reinstall/update preserves user-owned configuration and reports changes.
- [x] Tests and a real Codex session verify ticket isolation and logging.

## Worklog

- 2026-08-03 — Started in a native Codex subagent as requested. No owning-session intervention has been needed so far.
- 2026-08-03 — Verified the current official Codex contract before designing the package: plugins use `.codex-plugin/plugin.json`, bundle skills under `skills/`, and load lifecycle commands from `hooks/hooks.json`. Codex supports `SessionStart`, `SessionEnd`, `PreCompact`, `PostCompact`, `SubagentStart`, `SubagentStop`, `UserPromptSubmit`, `PostToolUse`, and `Stop`; every command receives JSON on stdin with the parent `session_id` and `cwd`, while subagent events add their agent identity. Plugin hooks receive `PLUGIN_ROOT` and require one-time trust review in `/hooks`.
- 2026-08-03 — Existing `kd plugin` only mutates repo-local Claude settings. The smallest Codex-compatible path is a bundled first-party plugin plus an idempotent personal marketplace installer that copies only Kingdom's managed plugin directory and merges only Kingdom's marketplace entry, preserving every user-owned entry and config file.
- 2026-08-03 — Implemented the plugin package and installer. The installed manifest gets a deterministic content hash in its SemVer build suffix, so changed skills/hooks invalidate Codex's cache while identical reinstalls make no writes. Unknown files inside the Kingdom plugin, unrelated marketplace entries, marketplace metadata, and `~/.codex/config.toml` are left alone. `kd plugin install codex` installs and activates through `codex plugin add kingdom@<marketplace>`, while `kd update` refreshes only an already-configured Kingdom plugin.
- 2026-08-03 — Focused package/CLI/update tests passed (44 tests before the all-event additions). Manual host check confirmed the installed Codex CLI exposes `codex plugin add`, `list`, `remove`, and `marketplace` as expected. In this real native-subagent session, `uv run kd tk current --id` returned exactly `d023`; `kd status` simultaneously showed this worker on `d023`, the owning Codex session on `ca34`, and the reviewed peasant on `92ca`, with no cross-session guessing. Ticket logging remains this direct Markdown worklog.
- 2026-08-03 — Final ticket-local evidence: 46 focused tests pass; Ruff lint and format checks pass; every bundled Codex lifecycle payload reaches the shared adapter without diagnostics; and the exact assembled artifact passes plugin-creator's official validator. A repository-wide run was attempted but briefly hit a collection mismatch while the concurrent `92ca` peasant tests were present before its implementation was integrated. The owning session has since completed overlapping `ca34` handlers and will run the combined full suite during review. No intervention was required for this native subagent's implementation.
- [08:55] — Closed: Implemented and verified the Codex plugin, lifecycle hooks, idempotent install/update path, and real session isolation.
