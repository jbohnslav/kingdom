---
id: "473d"
status: in_progress
deps: [09c8, 781f]
links: []
created: 2026-08-02T13:58:42Z
type: task
priority: 2
assignee: codex:a775e0448cdb5a76
parent: c759
---
# Build upgrade, rollback, and doctor support for existing .kd repositories

Use real repository shapes—including Kingdom's own long history—to make upgrades
boring and recoverable.

## Acceptance Criteria

- [ ] Migration fixtures include backlog, active, archived, old context, and partially written state.
- [ ] Upgrade is idempotent and preserves every ticket's Markdown content and ID.
- [ ] A rollback/backup path is documented and exercised.
- [ ] `kd doctor` detects ambiguous bindings, orphan contexts, bad resolutions, and stale host installs.
- [ ] Doctor suggests exact non-destructive repairs and never silently rewrites user content.

## Worklog

- [2026-08-03 13:29] [codex:a775e044] — Started implementation. Owner will integrate upgrade/rollback work while native subagents independently audit doctor diagnostics and migration fixture gaps.
- [2026-08-03 13:33] [codex:a775e044] — Audit confirms dependency 09c8 already owns the lazy context migration contract: exact v0.6 binding or one generic active ticket migrates idempotently; ambiguous state is refused; legacy JSON is retained. This ticket will close the operational gaps with mixed-location fixtures, rollback evidence, and read-only doctor diagnostics. Council thread council-38fc is reviewing diagnostic and repair semantics.
- [2026-08-03 13:34] [codex:a775e044] — Migration audit: focused legacy/state/current tests pass (168 passed). The existing lazy migration is narrower than a generic repository upgrader and rewrites the ticket through the serializer when replacing a generic assignee, so mixed-location fixtures must verify IDs, body/worklog content, unknown metadata behavior, crash/partial-state recovery, and second-run idempotency. The audit also found kd start can swallow the legacy .kd/runs guard; captured separately in backlog per dogfood policy.
- [2026-08-03 13:35] [codex:a775e044] — Doctor audit: current command only checks config and executable presence; nine existing doctor tests and 132 related context/resolution/plugin tests pass. Required implementation should raw-scan state (because existing collectors intentionally skip/dedupe malformed records), reuse validate_terminal_evidence, distinguish retained legacy bindings and legitimate parent/subagent sharing from ambiguity, compare Codex managed payload bytes, treat legacy Claude lifecycle coverage as stale, and emit additive structured repair guidance without writes. Also confirmed check_cli currently treats a nonzero version command as installed; this belongs in the host-health fix.
- [2026-08-03 13:37] [codex:a775e044] — Rollback/host-install audit: no public upgrade/rollback guide exists; doctor and plugin status currently check presence rather than managed-content freshness. Reviewer proposed a new transactional repository upgrade command, but this conflicts with the narrower accepted 09c8 lazy-migration contract and would substantially expand the release gate. Council thread council-38fc is resolving that scope fork. Regardless, this ticket needs public rollback guidance, read-only managed-content inspection, and byte-snapshot doctor tests.
- [2026-08-03 13:38] [codex:a775e044] — Red phase established with four expected failures. The mixed fixture proves lazy migration drops unknown frontmatter by canonicalizing the active ticket; crash-recovery rewrites that ticket again; doctor exits healthy despite ambiguous/orphan/invalid-resolution/stale-install state; and check_cli treats /usr/bin/false as installed. No implementation changes yet.
