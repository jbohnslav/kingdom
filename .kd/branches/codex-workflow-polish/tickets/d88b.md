---
id: "d88b"
status: closed
deps: []
links: []
created: 2026-08-03T15:46:10Z
type: task
priority: 2
closed_at: 2026-08-03T19:02:37Z
resolution: completed
closed_context: codex:a775e0448cdb5a76
assignee: codex:ca2f1a4221e5656a
---
# Align uv and pre-commit Ruff formatter versions

`uv.lock` currently resolves Ruff 0.15.0 while `.pre-commit-config.yaml` pins
`ruff-pre-commit` v0.8.6. The two formatters produce different layouts for the
same assertion expressions, so `uv run ruff format --check` can pass and the
commit hook can immediately rewrite the file in the opposite style.

## Acceptance Criteria

- [x] The uv-managed and pre-commit Ruff versions use one compatible formatter behavior.
- [x] Running the documented format check followed by the commit hook produces no rewrite.
- [x] Development documentation names the canonical lint/format commands.

## Worklog

- 2026-08-03 — Found while reviewing ticket `d456`: Ruff 0.15.0 formatted
  `tests/test_skill.py`, then the v0.8.6 pre-commit hook reformatted the same
  assertions back during commit. This is non-blocking for `329d`; preserve the
  hook output for the current commit and resolve version alignment separately.
- [2026-08-03 14:56] [codex:ca2f1a42] — Red/green checkpoint: uv.lock already resolved Ruff 0.15.0 while pyproject allowed ruff>=0.8.6 and ruff-pre-commit stayed at v0.8.6. Added a tooling contract first; it failed on the non-exact/mismatched pin and missing canonical format/hook documentation. Smallest alignment keeps the existing locked tool: pyproject now pins ruff==0.15.0, pre-commit uses v0.15.0, and uv lock changed only the project specifier metadata. README documents uv lint/format plus the two separate pre-commit hook commands. Focused contract passes 2/2; uv Ruff check/format and both newly installed pre-commit hooks pass on tests/test_tooling_config.py without rewriting it. Whole-tree verification is pending because a concurrent unrelated src/kingdom/cli/__init__.py edit currently has Ruff I001; d88b will not modify that work.
- [2026-08-03 15:02] [codex:ca2f1a42] — Final open/uncommitted handoff. Changed: pyproject.toml pins ruff==0.15.0; .pre-commit-config.yaml pins ruff-pre-commit v0.15.0; uv.lock changes only the Ruff project specifier; README documents canonical lint, format, and separate hook commands; tests/test_tooling_config.py prevents pin/guidance drift; tests/test_skill.py and tests/test_tui.py received only the required 0.15.0 assertion-layout normalization; release notes and their test now point d88b at its active branch ticket. Red proof: uv run pytest -q tests/test_tooling_config.py failed 2/2 before the fix; it passes 2/2 after. No-rewrite proof passed exactly: uv run ruff check .; uv run ruff format --check .; uv run pre-commit run ruff --all-files; uv run pre-commit run ruff-format --all-files; uv run ruff check .; uv run ruff format --check .; both hooks passed without modifications. Final uv run pytest -q tests/test_public_docs.py tests/test_tooling_config.py passed 13; uv run ruff check . passed; uv run ruff format --check . reported 99 files already formatted; git diff --check passed. Full suite attempt: 2166 passed, 38 skipped, 1 xfailed, 6 failed during concurrent neighbor edits; doctor and public-doc failures now rerun green, while tests/test_harness.py::TestRunAgentLoop::test_loop_fails_on_backend_error remains red during bc0c retry-semantics work. No failure waived. Leave d88b open and uncommitted for parent review.
- [2026-08-03 15:02] [codex:a775e044] — Parent review passed: full suite 2172 passed, 38 skipped, 1 expected failure; whole-tree Ruff check and format check passed; all pre-commit hooks passed without rewriting files.

## Lifecycle

- 2026-08-03T19:02:37Z [codex:a775e0448cdb5a76] — closed (completed)
