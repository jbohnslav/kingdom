---
id: "3732"
status: closed
deps: [2877]
links: []
created: 2026-03-06T12:56:28Z
type: task
priority: 2
closed_at: 2026-08-03T16:11:13Z
resolution: completed
closed_context: codex:a775e0448cdb5a76
assignee: codex:0f4ec9f976ec982b
parent: 329d
---
# Make kd design phase optional — skip straight to tickets for backlog sprints

## Acceptance Criteria

- [x] An existing backlog ticket can be pulled and started without creating or editing `design.md`.
- [x] New-work initialization offers tickets/epics as the default path.
- [x] Design commands remain available only as an optional/legacy path chosen by `2877`.
- [x] Help and tests cover both initialized and existing repositories.

## Worklog

- 2026-08-03: Started from committed ticket-first initialization (`0830384`). Scoped this ticket to the optional `kd design` entry point plus focused design, status, start, and backlog regressions; broad help/docs remain with sibling ticket `ab52`.
- 2026-08-03: Added a failing regression proving an initialized workspace without `design.md` should create the optional template only when `kd design` is invoked. Baseline failed with exit code 1 under the old missing-document error. Added explicit coverage that backlog pull-and-start leaves `design.md` absent.
- 2026-08-03: Changed the default `kd design` invocation to initialize the optional template on demand via `ensure_design_initialized`. Existing non-empty documents remain byte-for-byte unchanged; `design show` and the inert `design approve` compatibility flag retain their prior behavior.
- 2026-08-03: Focused design/start/backlog suite passed (7 tests) and Ruff passed. Manual dogfood in a fresh git repository pulled and started backlog ticket `2957` without a design artifact, showed ticket/context status without missing-planning warnings, then created `design.md` only after explicit `kd design`; a second invocation preserved the file hash.
- 2026-08-03: First full-suite run reached 2,096 passed, 38 skipped, and 1 expected failure. Its only four failures were concurrent sibling-ticket documentation regressions in the newly added `tests/test_public_docs.py`, pending that ticket's README/smoke/CI/release edits; all 3732-related modules were green. Full-suite rerun remains required after the concurrent work settles.
- 2026-08-03: Final full-suite rerun passed against the combined worktree: 2,100 passed, 38 skipped, and 1 expected failure in 22.81 seconds.

## Lifecycle

- 2026-08-03T16:11:13Z [codex:a775e0448cdb5a76] — closed (completed)
