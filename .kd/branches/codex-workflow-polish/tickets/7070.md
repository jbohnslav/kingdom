---
id: "7070"
status: closed
deps: [539b, 09c8]
links: []
created: 2026-08-02T13:57:37Z
type: task
priority: 2
closed_at: 2026-08-02T18:14:06Z
assignee: codex:fcce7f4353b26c01
parent: 029e
---
# Test three to five concurrent sessions on one branch

Exercise the exact daily failure mode: several agents and terminals, one branch,
different tickets, interleaved starts/logs/stops.

## Acceptance Criteria

- [x] An integration test creates at least five contexts on one branch with distinct tickets.
- [x] Each context's `current`, log target, status row, and close behavior stays isolated.
- [x] Reused terminals, ended sessions, and stale bindings are covered.
- [x] At least one test spans two host identity formats.
- [x] A manual reproduction script records readable CLI output for release evidence.

## Worklog

- 2026-08-02 — Added an end-to-end test with five contexts across `kingdom`,
  `codex`, and `terminal` hosts. Each context starts a distinct ticket, resolves
  its own `current`, logs only to that ticket, appears in JSON status, and can
  close without disturbing the remaining contexts. Added terminal reuse and
  stale/ended-session cases.
- 2026-08-02 — The first integration run exposed stale legacy resurrection:
  `status --prune-stale` removed the new context file, but `tk current` migrated
  the old terminal binding and recreated it. Pruning now removes matching legacy
  compatibility bindings too. Focused verification: 91 passed.
- 2026-08-02 — Ran this manual reproduction against the working-tree CLI in an
  isolated `/private/tmp/kd-context-dogfood.*` workspace:

  ```sh
  mkdir -p "$workspace/.kd"
  KD_BASE="$workspace" uv run kd start dogfood/concurrent
  # create five stubs, then start each with a distinct KD_CONTEXT,
  # CODEX_THREAD_ID, or TERM_SESSION_ID
  KD_BASE="$workspace" uv run kd status
  # run `kd tk current --id` again under each identity
  ```

  Readable status evidence:

  ```text
  Tickets: 0 open, 5 in progress, 0 in review, 0 closed, 0 ready (5 total)

  Sessions:
    terminal:ddbb4989 · terminal · agent · 1ff2 [in_progress] Session echo
    codex:c8863b34 · codex · subagent · 19d7 [in_progress] Session delta
    kingdom:a62b92b8 · kingdom · agent · b0fc [in_progress] Session charlie
    kingdom:0c5b41b0 · kingdom · subagent · 6dbe [in_progress] Session bravo
    kingdom:636cdbdd · kingdom · agent · 81e1 [in_progress] Session alpha
  ```

  Exact current mappings were `alpha→81e1`, `bravo→6dbe`, `charlie→b0fc`,
  `delta→19d7`, and `echo→1ff2`. Fresh-workspace initialization was confusing
  during the run, so backlog ticket `e2d0` records that separate issue.
- 2026-08-02 — Final verification passed: Ruff clean; full suite 1948 passed,
  38 skipped, 1 xfailed.
- [14:14] — Closed: Verified five isolated contexts across three host formats, terminal reuse, session close, stale pruning, and manual CLI output; full suite passes.
