---
id: "48dd"
status: closed
deps: []
links: []
created: 2026-08-02T13:57:00Z
type: epic
priority: 2
closed_at: 2026-08-03T15:21:52Z
resolution: completed
close_reason: "Completed explicit closure semantics, canonical pull/defer flows, compatibility paths, and legacy-safe migration"
closed_context: codex:c4c6c1e74805c884
---
# Explicit closure, backlog flow, and ticket mutation cleanup

Make ticket state say what actually happened. Pulling starts planned work; closing
records an outcome. Arbitrary file-location movement and overlapping note commands
should not masquerade as lifecycle concepts.

## Acceptance Criteria

- [x] `kd tk close` records a typed resolution and human-readable reason.
- [x] Completed, won't-do, duplicate, and superseded work remain distinguishable.
- [x] `kd done` accepts valid terminal resolutions and reports their breakdown.
- [x] `tk pull` remains the canonical backlog-to-active transition.
- [x] `tk move` and `add-note` have clear compatibility/deprecation paths.
- [x] Existing ticket Markdown remains readable and migrates without data loss.

## Children

`781f`, `1ad3`, `7c7d`, `0ddb`, `045e`, and `3af0`.

## Worklog

- 2026-08-03 — Completed all six child tickets. Ticket closure now records typed
  resolutions, structured reasons, and append-only lifecycle evidence; reopen and
  legacy Markdown behavior remain compatible. `kd done` validates and summarizes
  terminal outcomes. Backlog selection uses `tk pull`, branch-to-backlog deferral
  uses lifecycle-safe `tk defer`, and the overlapping `move`/`add-note` surfaces
  have explicit deprecation paths.

- 2026-08-03 — Parent review covered every child implementation and resulted in
  follow-up fixes for re-closing legacy tickets and preventing stale resolution
  display outside closed state. Final integrated verification passed 2079 tests
  with 38 skips and 1 expected failure. Ruff lint, formatting for all changed
  Python files, `git diff --check`, and manual help rendering for the affected CLI
  commands also pass.
- [11:21] [codex:c4c6c1e74805c884] — Closed: Completed explicit closure semantics, canonical pull/defer flows, compatibility paths, and legacy-safe migration

## Lifecycle

- 2026-08-03T15:21:52Z [codex:c4c6c1e74805c884] — closed (completed): Completed explicit closure semantics, canonical pull/defer flows, compatibility paths, and legacy-safe migration
