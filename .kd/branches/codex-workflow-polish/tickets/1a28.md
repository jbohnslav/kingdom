---
id: "1a28"
status: closed
deps: [d023, 9353]
links: []
created: 2026-02-26T20:24:23Z
type: task
priority: 2
closed_at: 2026-08-03T13:28:31Z
assignee: codex:fcce7f4353b26c01
parent: d3a5
---
# Skill installation only works for Claude — make it compatible with other backends

Generalize `kd update`/installation around host adapters. Claude remains supported;
Codex is a first-class target; Cursor behavior follows the capability spike rather
than assumed filesystem conventions.

## Acceptance Criteria

- [x] Host-specific skill destinations are detected and updated idempotently.
- [x] Existing user files are never overwritten silently.
- [x] Install output says which hosts were updated, skipped, or need manual action.
- [x] Claude and Codex paths have automated coverage.
- [x] Cursor is included only to the level justified by `9353`.

## Worklog

- 2026-08-03 — Audited the existing installer: it already copied to Claude and
  conditionally to Codex/Cursor, but returned one aggregate status and overwrote
  differing Markdown without provenance. Added per-host detection/reporting and
  a content-hash manifest. New installs are managed; unchanged legacy installs
  can be adopted; unknown or locally modified files are preserved and reported
  as requiring manual action.
- 2026-08-03 — Cursor remains skill-only when `~/.cursor` already exists. This
  intentionally does not claim full hook/plugin installation after spike `9353`.
  Missing Codex/Cursor roots are reported as skipped instead of being created on
  machines where those hosts have never been configured.
- 2026-08-03 — Verification: 79 focused init/update/Codex-plugin tests pass; the
  full suite passes with 2021 passed, 38 skipped, and 1 expected failure. A manual
  isolated `kd update` run clearly reported Claude updated, Codex skipped, Cursor
  skipped with its limited-integration note, and the aggregate summary.
- [09:28] — Closed: Generalized safe idempotent skill refresh reporting across Claude, Codex, and limited Cursor support; full suite passes.
