---
id: "2974"
status: closed
deps: [2877]
links: []
created: 2026-03-06T12:59:48Z
type: task
priority: 2
closed_at: 2026-08-03T16:14:39Z
resolution: duplicate
close_reason: "UX evidence was incorporated into the optional-design policy and implemented by eb2c and 3732"
closed_context: codex:a775e0448cdb5a76
assignee: codex:2b774495e85ef049
parent: 329d
duplicate-of: 2877
---
# Workflow friction: design phase feels wrong when working existing backlog tickets

When pulling existing backlog tickets into a branch, the design/breakdown phases feel redundant — the work is already scoped. The design phase is useful for new features where you're going from idea to tickets, but awkward when tickets already exist. Related to 3732 (optional design phase). Consider: maybe design is for epics/new features, and backlog sprints skip straight to execution.

## Acceptance Criteria

- [x] Pulling existing scoped work does not require design or breakdown state.
- [x] Status does not frame missing design artifacts as incomplete work.
- [x] The revised flow is manually dogfooded on a backlog sprint.
- [x] This ticket is closed with a typed resolution once `2877` determines whether it is input, duplicate, or implementation work.

## Worklog

- 2026-08-03 — Reviewed the committed policy and implementation record. Ticket
  `2877` explicitly classifies `2974` as validated UX input already captured by
  that policy—not separate implementation—and directs typed closure as
  `duplicate` of `2877`. It classifies `3732` separately as the implementation
  follow-up. This resolves the original product question without duplicating the
  optional-design work.

- 2026-08-03 — Committed evidence satisfies the first two criteria. Commit
  `0830384` (`eb2c`) makes `kd start` create/resume a ticket workspace without
  scaffolding or requiring `design.md`/`breakdown.md`, preserves any legacy files
  in place, and offers ticket/epic next actions. Commit `709c8b0` (`3732`) keeps
  design opt-in, creates it only on explicit `kd design`, and adds a regression
  proving `tk pull` plus `--start` leaves the planning files absent. Existing
  human-status coverage asserts that `Design: missing` and `Breakdown: missing`
  never appear; compatibility JSON keys remain stable without becoming gates.

- 2026-08-03 — Independently reran the four directly relevant committed tests:
  ticket-first start without planning docs, human status with no planning docs,
  backlog pull/start without a design, and optional design creation on demand.
  `uv run pytest -q` with those four node IDs passed 4/4.

- 2026-08-03 — Manually dogfooded the revised backlog sprint in fresh temporary
  Git repository `/private/tmp/kd-2974.1ghoYm` using the committed source CLI.
  `kd start backlog-sprint` reported a zero-ticket workspace and ticket/epic next
  action; filesystem inspection found no design or breakdown artifact. Created
  scoped backlog ticket `5b24`, then `kd tk pull 5b24` and context-bound
  `kd tk start 5b24` succeeded. Human `kd status` reported branch
  `backlog-sprint`, one in-progress ticket, and its concurrent execution context
  with no planning-phase or missing-artifact warning. A final filesystem check
  still found neither planning file.

- 2026-08-03 — Lifecycle-only handoff is ready. No code or documentation was
  changed for `2974`; the remaining unchecked criterion is intentionally the
  typed `duplicate` closure that the epic owner will perform after review, with
  `duplicate_of: 2877` and a reason that this ticket's UX evidence was absorbed
  by the completed policy and implementation.

- 2026-08-03 — Parent review accepted the evidence and closed this ticket with
  typed resolution `duplicate`, `duplicate_of: 2877`, and the reason that the UX
  evidence was incorporated into the optional-design policy and implemented by
  `eb2c` and `3732`.
- [12:14] [codex:a775e0448cdb5a76] — Closed: UX evidence was incorporated into the optional-design policy and implemented by eb2c and 3732

## Lifecycle

- 2026-08-03T16:14:39Z [codex:a775e0448cdb5a76] — closed (duplicate) [reference: 2877]: UX evidence was incorporated into the optional-design policy and implemented by eb2c and 3732
