---
id: "f122"
status: closed
deps: [e204]
links: []
created: 2026-08-02T13:57:37Z
type: task
priority: 2
closed_at: 2026-08-02T16:51:44Z
assignee: codex:fcce7f4353b26c01
parent: 029e
---
# Bind kd tk start and current to the active execution context

Replace "first in-progress ticket on this branch" with an exact binding for the
calling context. Branch membership remains useful organization, but not identity.

## Acceptance Criteria

- [x] `kd tk start ID` assigns the ticket to the resolved execution context.
- [x] `kd tk current` returns only that context's ticket and never guesses among peers.
- [x] An unbound context gets a concise recovery message with explicit next actions.
- [x] An intentional context override works for hooks and test automation.
- [x] Starting, switching, closing, and reopening tickets update bindings atomically.
- [x] CLI output is manually inspected in two simultaneous terminals.

## Worklog

- 2026-08-02 — Started after `e204` closed and unblocked this ticket. Added six
  failing CLI tests covering two isolated contexts, refusal to guess for an
  unbound context, context-ID assignment, explicit branch fallback, close cleanup,
  and switching between tickets. All six failed against the branch-global
  implementation, including session one incorrectly receiving session two's
  ticket and a switched session returning its first ticket.

- 2026-08-02 — Implemented exact context binding for start/current, context-ID
  assignment, explicit `--branch-fallback`, switch unassignment, and binding
  cleanup on close/reopen/status/delete. A temporary real CLI run created two
  simultaneous contexts; each returned only its own ticket, an unbound third
  context refused to guess, and explicit fallback remained available.

- 2026-08-02 — Dogfood caught two development issues. New `.kd` repositories did
  not ignore advisory `*.json.lock` files; added the missing template pattern and
  regression assertion. Bare `kd` in this checkout ran the installed release and
  produced plausible branch-global output instead of the working-tree behavior;
  filed active ticket `a431` and switched verification to `uv run kd`.

- 2026-08-02 — Final verification: Ruff check/format passed; full suite passed
  with 1,936 tests passed, 38 skipped, and 1 expected failure. The first full run
  caught one remaining `@hand` list assertion, which was updated to verify the
  deterministic context assignee before the clean rerun.
- [12:51] — Closed: Exact session-scoped start/current binding implemented and dogfooded with two concurrent contexts; full suite green
