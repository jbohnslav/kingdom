---
id: "063d"
status: closed
deps: []
links: []
created: 2026-08-03T18:18:59Z
type: task
priority: 2
closed_at: 2026-08-03T18:52:06Z
resolution: completed
close_reason: "Exact-owner worklog activity now refreshes context freshness without masking staleness from foreign commands."
closed_context: codex:a775e0448cdb5a76
assignee: codex:a775e0448cdb5a76
---
# Refresh context activity on owner ticket commands

## Acceptance Criteria

- [x] A ticket command run by the ticket's exact owning context refreshes that
  context's `last_seen` timestamp when the command demonstrates activity.
- [x] At minimum, `kd tk log` refreshes activity; decide and document whether
  other owner-scoped mutations should do the same.
- [x] Commands run by a different context for that ticket do not keep the owner
  artificially live.
- [x] `kd status` does not mark a diligently logging long-running owner stale
  merely because it has not rerun `kd tk current` or `kd tk start`.
- [x] Focused tests cover exact-owner activity, foreign-context logging, and the
  stale threshold.

## Context

Ticket `b507` dogfooding used three real contexts on one branch. After this
context ran several `kd tk log b507` commands, `kd status --stale-hours 0.01`
still marked it stale because `ticket_log` resolves the context only to label the
author and never persists its new `last_seen`. Running `kd tk current --id`
immediately made only this owner live again. The output is internally consistent,
but it makes a session following the documented durable-log loop look inactive;
with the default 24-hour threshold the same false stale classification affects
long-lived sessions.

Do not refresh activity from arbitrary reads or another worker's log against the
same ticket. The update must preserve exact context ownership semantics.

## Worklog

- [2026-08-03 14:46] [codex:a775e044] — Implemented exact-owner activity refresh after successful worklog appends. Red phase: two focused tests failed because the owner timestamp stayed two days old and status marked it stale. Green phase: exact owner logs refresh the active binding, foreign logs leave it untouched, and status reports the diligently logging owner live. Focused state/current/worklog verification: 111 passed; Ruff, format, and diff checks pass.
- [2026-08-03 14:52] [codex:a775e044] — Parent review confirmed activity refresh occurs only after a successful owner worklog append and only when the exact active binding still names that ticket; foreign logs and reads do not refresh it. Integrated gates: 2162 passed, 38 skipped, 1 xfailed; full Ruff and documented smoke passed.
- [14:52] [codex:a775e0448cdb5a76] — Closed: Exact-owner worklog activity now refreshes context freshness without masking staleness from foreign commands.

## Lifecycle

- 2026-08-03T18:52:06Z [codex:a775e0448cdb5a76] — closed (completed): Exact-owner worklog activity now refreshes context freshness without masking staleness from foreign commands.
