---
id: "671a"
status: in_review
deps: []
links: []
created: 2026-03-03T12:35:58Z
type: feature
priority: 2
assignee: peasant-671a
---
# Add kd update command: self-upgrade CLI + refresh skill files

Add a top-level kd update command that does two things in sequence: (1) runs `uv tool upgrade kingdom-cli` to update the installed CLI, and (2) calls `install_skill()` to refresh the Claude skill files. This eliminates the need to run `kd start --force` after upgrading, which conflates session lifecycle with maintenance. The command should report what it did (upgrade result, skill files refreshed). Keep `install_skill()` running automatically in `kd start` as well — `kd update` is the explicit recovery/upgrade path.

## Acceptance Criteria

- [ ] kd update runs `uv tool upgrade kingdom-cli` and reports the result
- [ ] kd update calls install_skill() to refresh skill files after upgrade
- [ ] kd update prints a summary of what it did (upgrade result, skill status)
- [ ] kd start still auto-refreshes skill files (no regression)
- [ ] kd update appears in kd --help output

## Worklog

- [08:12] — Iteration 1/50 — calling agent
  Ticket: Add kd update command: self-upgrade CLI + refresh skill files
- 2026-03-03 08:24 — Implemented kd update command in cli/__init__.py: runs uv tool upgrade kingdom-cli, then install_skill(). Reports status with checkmarks/crosses, summary panel. 5 tests added (success, upgrade failure, uv not found, dev symlink skip, help visibility). All 1535 tests pass, ruff clean.
- [08:24] — Now let me read the key files to understand the exact code I need to modify.Now let me check the existing tests for CLI commands to understand the testing pattern.Good, I have a clear picture of the codebase. Let me implement the `kd update` command.Those diagnostics are pre-existing (unrelated t...
- [08:24] — Files changed:
  src/kingdom/cli/__init__.py | 54 +++++++++++++++++++++++++++++++
   tests/test_cli.py           | 79 ++++++++++++++++++++++++++++++++++++++++++++-
   2 files changed, 132 insertions(+), 1 deletion(-)
- [08:26] — Council review: BLOCKING (bounce 1/3) — returning to working
  [claude] APPROVED: Let me review this ticket and its implementation.Let me check the surrounding context — imports and the existing pattern for subprocess usage and `install_skill`.Now let me check the package name — the ticket says `kingdom-cli` but let me verify that's correct.Good, package name is `kingdom-cli` — matches the command. Let me now look at the duplicate `is_symlink` check pattern and verify one more edge case.Now let me check whether the test mocking pattern is correct — specifically t...
  [codex] BLOCKING: Using the `kingdom` skill for this review because this is a ticket-level `kd` CLI change. I’m reading the ticket, the commit, and the touched files first, then I’ll run the relevant tests and lint checks against the proposed patch before I judge it.
- [08:26] — Iteration 2/50 — calling agent
  Bouncing on council feedback (1 blocking, 1 approved) — see review above
- 2026-03-03 08:28 — Council bounce fix: install_skill() now returns bool (True=ok, False=error). update command checks return value — no longer claims success when skill refresh fails. Added test_update_skill_refresh_failure. 1536 tests pass, ruff clean, pre-commit hooks pass.
- [08:29] — Now let me read the test file to see the current tests.Good. The codex reviewer is right — `install_skill()` swallows errors and just prints a warning, but `update` unconditionally claims success. The fix: change `install_skill()` to return a `bool` indicating success, and use that in `update` to...
- [08:29] — Files changed:
  src/kingdom/cli/__init__.py |  9 ++++++---
   src/kingdom/cli/helpers.py  |  8 ++++++--
   tests/test_cli.py           | 23 ++++++++++++++++++++---
   3 files changed, 32 insertions(+), 8 deletions(-)
