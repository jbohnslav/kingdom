---
id: "6b9d"
status: closed
deps: []
links: []
created: 2026-08-24T20:21:28Z
type: task
priority: 2
closed_at: 2026-09-05T17:16:57Z
resolution: completed
close_reason: "completed"
closed_context: codex:73b41ca6e06f778e
assignee: codex:73b41ca6e06f778e
parent: b1de
---
# Address PR 56 reliability review findings

## Acceptance Criteria

- [x] Queued Council deliveries remain durable until their dispatched round completes and recover correctly after interruption
- [x] Completed targets are persisted per delivery so restart resumes only unfinished Council members
- [x] Persisted responses reconcile the completion ledger after a crash without redispatching the provider
- [x] Doctor reports linked-worktree configuration resolution failures in human and JSON output without traceback
- [x] Restarted deliveries preserve the original eligible target schedule so members muted at submission are not queried in resumed broadcast, natural, or round-robin auto-turns
- [x] A restart regression fails before the fix and passes afterward across every affected chat mode
- [x] Focused regressions, Ruff, format, pre-commit, smoke, the full test suite, and `uv run kd status --check` pass at the final PR head
- [x] Pinned models for providers without catalog discovery remain quietly unchecked instead of producing a misleading warning
- [x] `kd council review` reports missing active-session guidance without a traceback
- [x] Queued deliveries persist and resume with the chat mode and auto-round count selected at submission
- [x] Config show and doctor resolve the repository-owner config path once per invocation
- [x] Fresh regressions fail before the fixes and the complete verification suite passes at the final review head

## Worklog

- [2026-08-24 16:24] [codex:f1fe48bc] — Reproduced both PR review findings with failing regressions. A pending delivery whose King prompt already exists is discarded during restart instead of remaining queued for dispatch. `kd doctor` lets `config_source_path()` escape as `ValueError`, producing no human or JSON diagnostic. Focused run: 3 tests failed exactly on those behaviors before implementation.
- [2026-08-24 16:26] [codex:f1fe48bc] — Implemented both PR review fixes. `src/kingdom/tui/app.py` now keeps the head delivery in the durable queue until the awaited Council dispatch completes; restart reuses a persisted King prompt without rendering or writing it twice, then dispatches the unfinished round. `src/kingdom/cli/__init__.py` now converts config-source resolution `ValueError` into normal human and JSON doctor diagnostics. Verification: focused regressions passed, all 38 Textual integration tests passed, `uv run pytest` passed with 2275 passed / 41 skipped / 1 xfailed, pre-commit passed, smoke passed, and manual `uv run kd doctor` output was inspected.
- [2026-08-24 20:48] [codex:f1fe48bc] — Reproduced the follow-up P1 before implementation. The pending-delivery model cannot round-trip completed targets, and a real restart with Claude already persisted re-queries Claude, producing a duplicate fourth transcript message instead of querying only Codex. Both focused regressions failed on the current code.
- [2026-08-24 20:52] [codex:f1fe48bc] — Resolved the follow-up P1 with a durable per-member occurrence ledger and persisted first-exchange state. Restart reconstructs the original delivery schedule, skips completed query occurrences, and resumes only unfinished members without duplicating transcript responses or provider calls; repeated round-robin occurrences are covered explicitly. Verification: the two original failing regressions now pass, the added round-robin restart regression passes, all 40 Textual integration tests pass, `uv run pytest` passes with 2276 passed / 43 skipped / 1 xfailed, pre-commit passes, smoke passes, and `git diff --check` passes.
- [2026-08-26 10:02] [codex:f1fe48bc] — Confirmed the follow-up P2 is a real crash-consistency gap: `run_query()` persists a provider response before separately updating the pending delivery ledger. A hard exit between those writes leaves durable proof of the response but causes restart to redispatch and duplicate the transcript/provider charge. The narrow fix will tag response messages with the active delivery ID and reconcile completed member occurrences from those durable messages before scheduling recovery.
- [2026-08-26 10:04] [codex:f1fe48bc] — Added the crash-window regressions before implementation. The focused run failed in both expected ways: `run_query()` persisted no delivery ID, and restart queried Claude again despite a tagged durable response, producing a third transcript message.
- [2026-08-26 10:08] [codex:f1fe48bc] — Resolved the follow-up P2 with a narrow durable-response reconciliation. Council responses now carry the active delivery ID; restart counts tagged persisted responses against the completion ledger and skips any provider occurrence whose response was written before a hard exit. This also preserves repeated round-robin occurrence counts. Regression evidence: both new tests failed before implementation and passed afterward. Final verification: 3 focused recovery tests passed; `uv run pytest -q` passed with 2276 passed / 44 skipped / 1 xfailed; all 41 Textual integration tests passed; Ruff, pre-commit, smoke, and `git diff --check` passed.
- [2026-09-05] [codex] — Reopened after the final Claude review identified a distinct recovery gap at https://github.com/jbohnslav/kingdom/pull/56#issuecomment-5400758189. `CouncilChatApp` initializes `self.muted` empty on restart, while resumed broadcast, natural, and round-robin auto-turns recompute eligibility from live `self.muted` instead of the delivery's persisted target snapshot. A member muted when the King submitted the delivery can therefore receive an unintended resumed provider call. Ticket 8096 covers unavailable configured targets and does not cover this mute-snapshot bug.
- [2026-09-05] [codex] — Added a parameterized Textual restart regression covering broadcast, natural, and round-robin deliveries whose persisted targets exclude a muted Codex member. The red-first run failed all three cases at the expected assertion: the resumed app queried Codex once despite Codex being absent from the delivery target snapshot.
- [2026-09-05] [codex] — Implemented the narrow scheduling fix: broadcast auto-rounds and sequential natural/round-robin auto-turns now reuse the delivery's persisted target list instead of rebuilding eligibility from `self.member_names` and the restarted app's empty `self.muted` set. The same three-case regression now passes, proving the excluded member receives no provider call after restart.
- [2026-09-05] [codex] — Verification before closure is green. The focused red-first regression passed 3/3 after implementation; `tests/test_tui.py` passed 159 tests; all 44 Textual integration tests passed; the full standard suite passed with 2,276 passed, 47 skipped, and 1 expected xfail; repository-wide Ruff check and format check passed; pre-commit passed every hook; the documented smoke workflow passed; and `git diff --check` passed. The initial full-suite run exposed stale editable 1.0.0 metadata and legacy partial local Claude hooks; `uv sync --all-groups` updated the environment to 1.0.1 and `uv run kd plugin enable` applied the documented local hook repair, after which the affected CLI tests and full suite passed.
- [2026-09-05] [codex] — Post-closure readiness gate passed: `uv run kd status --check` reports nine closed tickets, zero nonterminal tickets, and `Readiness: ready` on `codex/b1de-council-reliability`.
- [2026-09-05] [codex] — Reopened for the next automated review cycle. Fresh Claude/Codex comments identify four additional findings: a misleading pinned-Claude model warning, an unhandled missing-session error in `council review`, queued deliveries that do not snapshot mode/auto-rounds, and repeated linked-worktree config-source discovery in config show/doctor. Adding focused red-first coverage before implementation.
- [2026-09-05 13:14] [codex:73b41ca6] — Added focused regressions for all four fresh review findings before implementation. The red-first run reproduced each issue: pinned Claude models returned a misleading catalog warning, council review surfaced a raw RuntimeError, doctor/config show resolved the config source four/three times, and resumed deliveries used the live schedule (3 vs 1 calls and 1 vs 2 calls).
- [2026-09-05 13:14] [codex:73b41ca6] — Implemented the narrow fixes. Unsupported provider catalogs are quietly unchecked; council review now converts missing-session RuntimeError into normal CLI guidance; config loaders accept one pre-resolved path threaded through config show and doctor; and queued deliveries persist chat_mode/auto_rounds and dispatch from that snapshot, with legacy queue entries hydrated once. All seven focused regressions now pass.
- [2026-09-05 13:16] [codex:73b41ca6] — Final verification for the fresh review cycle is green: 432 focused CLI/config/TUI tests passed; all 46 Textual integration tests passed; the full standard suite passed with 2,280 passed, 49 skipped, and 1 expected xfail; repository-wide Ruff check/format, pre-commit, smoke, and git diff validation passed. Manual config show, doctor, and no-session council review output were inspected; doctor no longer shows a catalog warning for pinned Claude models, and council review prints clean recovery guidance.
- [13:16] [codex:73b41ca6e06f778e] — Closed: completed

## Lifecycle

- 2026-08-24T20:27:09Z [codex:f1fe48bc0e4e0556] — closed (completed)
- 2026-08-25T00:45:30Z [codex:f1fe48bc0e4e0556] — reopened (previous: completed)
- 2026-08-25T00:53:08Z [codex:f1fe48bc0e4e0556] — closed (completed)
- 2026-08-26T14:02:01Z [codex:f1fe48bc0e4e0556] — reopened (previous: completed)
- 2026-08-26T14:08:41Z [codex:f1fe48bc0e4e0556] — closed (completed)
- 2026-09-05T16:48:42Z [codex:73b41ca6e06f778e] — reopened (previous: completed)
- 2026-09-05T16:54:34Z [codex:73b41ca6e06f778e] — closed (completed)
- 2026-09-05T17:11:12Z [codex:73b41ca6e06f778e] — reopened (previous: completed)
- 2026-09-05T17:16:57Z [codex:73b41ca6e06f778e] — closed (completed): completed
