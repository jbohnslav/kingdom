---
id: "953a"
status: closed
deps: []
links: []
created: 2026-03-05T16:22:15Z
type: task
priority: 2
closed_at: 2026-08-24T19:39:28Z
resolution: completed
closed_context: codex:f1fe48bc0e4e0556
assignee: kingdom:09b49a905ee41384
parent: b1de
---
# kd council chat should support queued messages

## Acceptance Criteria

- [x] Follow-up messages submitted during an active exchange are acknowledged immediately without interrupting or replacing that exchange.
- [x] Pending messages are delivered in FIFO order without exposing later prompts to earlier Council turns.
- [x] Acknowledged pending messages survive application exit and resume from thread-local durable state on restart.
- [x] Promotion into normal thread history is reconciled by stable delivery ID so polling or restart cannot duplicate King messages or dispatch.
- [x] Focused unit and mounted Textual regressions cover live queueing, restart recovery, ordering, prompt isolation, and UI deduplication.

## Worklog

- 2026-03-05 11:23 — Context: when council members are thinking (async), the King should be able to queue follow-up messages without interrupting the current response. Same UX as Claude Code / Codex where you can type while the agent is working and it gets appended.
- [2026-08-24 15:16] [codex:0a19fefd] — Started focused implementation of queued Council chat delivery; inspecting current TUI delivery semantics and will add a failing regression before the fix.
- [2026-08-24 15:17] [kingdom:09b49a90] — Bound this ticket to an explicit native-subagent execution context after discovering inherited CODEX_THREAD_ID collision; captured the workflow defect as backlog ticket bc87.
- [2026-08-24 15:19] [kingdom:09b49a90] — Confirmed the regression before implementation: `uv run pytest --run-textual-integration tests/test_tui_integration.py::TestSendLifecycle::test_follow_up_queues_behind_active_exchange -q` failed because the expected six-message transcript never materialized. Current `send_message` increments `generation`, launches overlapping workers, and `run_query` discards the first exchange as stale.
- [2026-08-24 15:23] [kingdom:09b49a90] — Implemented FIFO Council chat delivery in `src/kingdom/tui/app.py`. Submissions render immediately and show a queue notification while an exchange is active, but persistence and dispatch happen only when each message reaches the queue head; this keeps thread files and agent prompts ordered and prevents overlapping reuse of member stream paths. Polling suppresses the exact already-rendered King sequence while continuing to drain intervening member responses normally.

  Regression coverage now sends two follow-ups while both members are thinking and verifies three ordered `king → member → member` transcript groups, prompt isolation between queued turns, one UI panel per King message, and no second worker/waiting-panel replacement for queued input.

  Verification: `uv run pytest tests/test_tui.py -q` (157 passed); `uv run pytest --run-textual-integration tests/test_tui_integration.py -q` (36 passed); focused three-message queue regression (passed); targeted `uv run ruff check`, `uv run ruff format --check`, and `git diff --check` (passed). No real provider CLIs were invoked; the Textual pilot test exercises the mounted UI, delayed concurrent members, persistence, polling, and generated prompts. Full suite intentionally left to the owning session.
- [2026-08-24 15:24] [kingdom:09b49a90] — Final diff check exposed whitespace-only lines from multiline tk log; cleaned the ticket and captured the CLI defect as backlog ticket 8828.
- [2026-08-24 15:25] [kingdom:09b49a90] — Owner review identified that acknowledged pending input was only in memory and could be lost on process exit; continuing with a thread-scoped durable queue and restart/no-duplicate regressions.
- [2026-08-24 15:27] [kingdom:09b49a90] — Red-first restart regression failed as expected: acknowledged queued follow-ups produced no thread-local pending file, proving they cannot survive ChatApp process exit.
- [2026-08-24 15:29] [kingdom:09b49a90] — Durable queue follow-up complete: thread-local pending JSON is atomically written before UI acknowledgement, loaded and resumed on mount, and promoted messages carry delivery IDs so restart reconciles the write/remove crash window without duplicate messages. Restart tests verify FIFO order, prompt isolation, restored UI panels, pending-file cleanup, and no re-query of an already-promoted message. Verification passed: 211 thread/TUI unit tests, 38 Textual integration tests, targeted Ruff/format checks, and git diff --check. Full suite remains for the owner.
- [2026-08-24 15:39] [codex:f1fe48bc] — Owner review complete. Combined verification passed: uv run pytest (2254 passed, 41 skipped, 1 xfailed), uv run pytest --run-textual-integration tests/test_tui_integration.py (38 passed), repository-wide Ruff check and format check, and git diff --check.

## Lifecycle

- 2026-08-24T19:39:28Z [codex:f1fe48bc0e4e0556] — closed (completed)
